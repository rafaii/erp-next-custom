# Changelog

Append-only log of every change. Format:
`YYYY-MM-DDTHH:MM:SSZ | domain | description | author | branch`

`description` MUST be 1-2 sentences: what changed, and for a fix, the root
cause in a few words. Nothing longer — verification detail, root-cause
narratives, and cleanup notes belong in the feature file's Implementation
Plan/Acceptance Criteria, not here.

## 2026-08-16

- `2026-08-16T08:33:11Z | custom-module | Scaffolded real_estate_os app under apps/, initialized git, and linked the GitHub remote with an initial commit. | developer-1 | main`
- `2026-08-16T08:33:11Z | vault | Restructured the repo around apps/real_estate_os and created the vault structure (4 draft ADRs, 7 domain indexes, 20 feature files, INDEX/DECISIONS/IMPLEMENTATION-PLAN). | developer-1 | main`
- `2026-08-16T08:45:08Z | vault | Approved ADRs 0001-0004 (repo-structure, esign, multi-tenancy, recurring-invoicing) and updated DECISIONS.md and feature references; vault confirmed local-only. | developer-1 | main`
- `2026-08-16T08:45:08Z | custom-module | Pushed the initial scaffold to GitHub (main @ 2caf125). | developer-1 | main`
- `2026-08-16T09:30:00Z | custom-module | Phase 1: added Building/Unit/Lease Agreement DocTypes, Customer KYC fixtures, and a bulk unit generator; installed and smoke-tested on the dev site. | developer-1 | agent/developer-1/core-doctypes`
- `2026-08-16T10:00:00Z | esign | Phase 2 slice 1: added E-Sign Document/Signer/Field/Audit Log DocTypes with auto-computed document hash and audit timestamps, registered as the E-Sign module. | developer-1 | agent/developer-1/esign-doctypes`
- `2026-08-16T10:30:00Z | custom-module | Added Workspace records for Real Estate and E-Sign so both modules appear in the Desk sidebar. | developer-1 | agent/developer-1/workspaces`
- `2026-08-16T11:00:00Z | vault | Updated IMPLEMENTATION-PLAN statuses and added HANDOFF.md as a compact resume point. | developer-1 | main`
- `2026-08-16T10:24:10Z | esign | Phase 2 slice 2: built the full e-sign workflow — guest OTP/consent/signature page, whitelisted signing endpoints, PDF stamping with a Certificate of Completion, and audit instrumentation — verified end-to-end. | developer-1 | agent/developer-1/esign-workflow`
- `2026-08-16T20:07:24Z | payments-accounting | Implemented recurring invoicing (ADR-0004): Rent Schedule child table plus payments/invoicing.py to build schedules, create invoices, and send due-date reminders, wired into the daily scheduler and verified live. | developer-1 | agent/developer-1/recurring-invoicing`
- `2026-08-16T23:19:17Z | esign | Lease Agreement now shows a "Send for e-signature?" confirmation dialog after save instead of doing nothing silently. | developer-1 | agent/developer-1/esign-send-confirm`
- `2026-08-16T23:27:39Z | esign | Fixed a Jinja crash on invalid/expired e-sign links by guarding the script block so it renders a friendly error instead of a traceback. | developer-1 | agent/developer-1/fix-esign-template`
- `2026-08-16T23:30:14Z | esign | Fixed re-sending an e-sign invite invalidating the previously emailed link — the signing token is now kept stable across re-sends. | developer-1 | agent/developer-1/fix-esign-resend-token`
- `2026-08-16T23:40:00Z | esign | Fixed the e-sign consent checkbox becoming permanently disabled after a failed consent attempt by gating it on OTP verification and resetting it on failure. | developer-1 | agent/developer-1/fix-esign-consent-gating`
- `2026-08-16T23:57:31Z | esign | Fixed invoice creation failing for guest e-sign signers by running _trigger_recurring_invoicing as Administrator instead of the permission-limited Guest user. | developer-1 | agent/developer-1/fix-esign-invoice-perms`
- `2026-08-17T00:01:40Z | esign | OTP emails now send immediately instead of being queued, cutting delivery from ~3 minutes to ~3 seconds. | developer-1 | agent/developer-1/otp-immediate-email`
- `2026-08-17T00:10:35Z | esign | Fixed the Certificate of Completion showing blank Final Hash/Completed On fields — they were computed after the certificate was rendered and the hash was circular; both are now computed correctly beforehand. | developer-1 | agent/developer-1/fix-cert-metadata`
- `2026-08-17T00:57:49Z | esign/payments-accounting | Approved ADR-0005 (two-party counter-signature + PDC-driven invoicing) and created the counter-signature and pdc-processing feature files. | developer-1 | main`
- `2026-08-17T01:29:05Z | esign/payments-accounting | Implemented ADR-0005: two-party sequential signing (customer then business signatory) with per-signature audit events, plus PDC-driven Rent Schedule generation with a monthly fallback; verified end-to-end. | developer-1 | agent/developer-1/counter-signature-pdc`
- `2026-08-17T01:47:30Z | custom-module | Added a dedicated Lease Agreement print format (parties, premises, term, schedule, signatures) as the DocType's default print format. | developer-1 | agent/developer-1/lease-print-format`
- `2026-08-17T03:07:31Z | custom-module | Added a lease/unit activity feed and wired Unit status to the lease lifecycle (Vacant→Reserved→Occupied, released on deletion), blocking a second lease on an already-leased unit. | developer-1 | agent/developer-1/lease-unit-activity`
- `2026-08-17T04:31:34Z | custom-module/payments-accounting | Added a Cancel Contract action that cancels pending PDCs/schedule rows, voids the e-sign document, releases the unit, and logs activity. | developer-1 | agent/developer-1/cancel-contract`
- `2026-08-17T04:43:42Z | custom-module | Fixed units staying Occupied after all their contracts were cancelled — release logic now only counts Draft/Sent/Signed leases as active. | developer-1 | agent/developer-1/fix-release-unit`
- `2026-08-17T05:23:34Z | custom-module | Added a 1000-unit cap and a confirmation step to bulk unit generation after an accidental 21,000-unit generation, and cleaned up the excess units. | developer-1 | agent/developer-1/unit-gen-guard`
- `2026-08-17T05:41:42Z | esign | E-sign invite emails now send immediately instead of being queued, so on-site customers get the signing link instantly. | developer-1 | agent/developer-1/immediate-esign-email`
- `2026-08-17T05:48:00Z | esign | Fixed the e-sign page prematurely showing "signed and finalized" with a broken download link for a two-party signer whose counterpart hadn't signed yet — it now checks document-level completion, not the individual signer's status. | developer-1 | agent/developer-1/esign-pdf-state`
- `2026-08-17T05:54:40Z | esign | Counter-signature invite emails now use different wording than the first invite, naming who has already signed. | developer-1 | agent/developer-1/counter-sign-email`
- `2026-08-17T05:58:41Z | esign | Fixed signer lookup failing when two signers share the same email — it now checks all signers by email+token instead of stopping at the first email match. | developer-1 | agent/developer-1/fix-signer-lookup`
- `2026-08-17T06:12:00Z | esign | Signing portal now shows a Signing Parties & Signatures card and a full audit trail table; the finalized PDF's Certificate of Completion includes the structured audit trail. | developer-1 | agent/developer-1/cert-audit-trail`
- `2026-08-17T07:00:00Z | vault | Synced HANDOFF.md and implementation-plan statuses with the actual shipped state (print format, activity feed, cancel contract, bulk guard, cert audit trail). | developer-1 | main`
- `2026-08-19T20:29:37Z | ui-portal | Added the initial admin portal UI: a config-driven sidebar and section views (Vue 3 SPA). | developer-1 | agent/imran/admin-portal-ui`
- `2026-08-19T20:52:45Z | ui-portal | Renamed the __PORTAL__ global to portalBootstrap to avoid a Jinja illegal-template error. | developer-1 | agent/imran/admin-portal-ui`
- `2026-08-19T21:08:08Z | ui-portal | Admin portal now serves from the site root via the home_page hook. | developer-1 | agent/imran/admin-portal-ui`
- `2026-08-19T21:24:30Z | ui-portal | Added demo data seeding and a detail view with linked records. | developer-1 | agent/imran/admin-portal-ui`
- `2026-08-20T06:13:39Z | ui-portal | Replaced the Vue admin portal with a new React 19 + Tailwind v4 + shadcn/ui frontend (9-section nav). | developer-1 | agent/imran/admin-portal-ui`
- `2026-08-20T06:30:00Z | vault | Approved ADR-0007 (React portal frontend) and marked the admin-portal-ui feature done. | developer-1 | agent/imran/admin-portal-ui`
- `2026-08-20T06:31:00Z | custom-module | Added the Landlord DocType (LLD-##### naming) with seeded demo data and a Building.landlord link. | developer-1 | agent/imran/admin-portal-ui`
- `2026-08-20T06:32:00Z | maintenance | Added the Maintenance Request DocType (MNT-2026-##### naming) with a Maintenance Spare child table, linked to Customer/Unit. | developer-1 | agent/imran/admin-portal-ui`
- `2026-08-21T23:00:48Z | ui-portal | Applied Aetris branding, a /dashboard route prefix, and the Garet heading font across the portal. | developer-1 | agent/imran/admin-portal-ui`
- `2026-08-21T23:20:36Z | ui-portal | Applied Garet to all headings and Inter for body text portal-wide, with lighter-weight headings and accent icons. | developer-1 | agent/imran/admin-portal-ui`
- `2026-08-21T23:54:28Z | ui-portal | Fixed the detail view breaking for every resource (undefined URL, garbled fields, missing linked records). | developer-1 | agent/imran/admin-portal-ui`
- `2026-08-22T03:48:42Z | ui-portal | Building and Unit records can now be created directly from the portal UI. | developer-1 | agent/imran/admin-portal-ui`
- `2026-08-22T06:19:59Z | ui-portal | Building's Units list now shows unit status, bed/bath count, and balcony. | developer-1 | agent/imran/admin-portal-ui`
- `2026-08-22T06:52:40Z | ui-portal | Fixed a 500 error on the Building detail page's Linked Records section. | developer-1 | agent/imran/admin-portal-ui`
- `2026-08-22T07:13:42Z | ui-portal | Wired the existing generate_units API into the Building detail page for bulk unit creation from the portal. | developer-1 | agent/imran/admin-portal-ui`
- `2026-08-22T10:28:53Z | ui-portal | Records (Properties, Units, Tenants, Landlords, etc.) can now be edited from the detail view. | developer-1 | agent/imran/admin-portal-ui`
- `2026-08-22T10:59:35Z | ui-portal | Fixed the sidebar active item and breadcrumb being wrong on detail pages. | developer-1 | agent/imran/admin-portal-ui`
- `2026-08-22T11:15:48Z | ui-portal | Switched to real (non-hash) portal URLs: BrowserRouter replaces HashRouter, and website_route_rules now derives an explicit route list from portal_nav_items instead of a catch-all that broke /login and /esign. | developer-1 | agent/imran/admin-portal-ui`
- `2026-08-22T11:42:26Z | ui-portal/esign | Split "create tenant" from "create lease for an existing tenant"; added a New Lease dialog (Manual or in-built e-sign) and a LeaseSigningPanel driving Draft→Sent→Signed. | developer-1 | agent/imran/admin-portal-ui`
- `2026-08-23T02:23:58Z | custom-module/ui-portal | Added cleaner customer codes (CUST-2026-##### naming) and the Portal Field Visibility DocType for configurable, drag-to-reorder per-doctype field visibility. | developer-1 | agent/imran/admin-portal-ui`
- `2026-08-23T04:16:42Z | custom-module | Fixed a naming-series patch not taking effect because updating the Selling Settings Single via db.set_value skipped on_update(), leaving the cached default stale; the patch now saves through get_single(). | developer-1 | agent/imran/admin-portal-ui`
- `2026-08-23T05:42:46Z | ui-portal | Added a "New lease" action directly on the tenant (Customer) detail page so an existing renter can get a second lease without re-creating the tenant. | developer-1 | agent/imran/admin-portal-ui`
- `2026-08-23T07:08:28Z | ui-portal | Fixed the New Lease unit dropdown showing the raw building code instead of a readable building name. | developer-1 | agent/imran/admin-portal-ui`
- `2026-08-23T07:19:25Z | esign/ui-portal | Business Signatory (required before send-for-signature) is now configurable from the portal Settings page instead of only in the Desk. | developer-1 | agent/imran/admin-portal-ui`
- `2026-08-23T07:44:15Z | esign/ui-portal | Surfaced e-sign send failures with an error banner and a Retry button, added an activity timeline via Comments, and added a portal-native Email Accounts panel under Settings. | developer-1 | agent/imran/admin-portal-ui`
- `2026-08-23T07:58:56Z | custom-module | Fixed an infinite redirect loop on /login and /esign caused by the website_route_rules catch-all; scoped to an explicit route list. | developer-1 | agent/imran/admin-portal-ui`
- `2026-08-23T08:14:48Z | custom-module/ui-portal | Fixed monthly rent silently saving as 0 on New Lease (no Rent field existed and fetch_from isn't enforced server-side) and fixed raw doctype IDs showing instead of display names on the contract page/PDF. | developer-1 | agent/imran/admin-portal-ui`
- `2026-08-23T08:38:55Z | esign | Added lease term-locking: once esign_status is Signed, term fields become immutable (only Cancel allowed); editing a Sent lease's terms now nullifies the old signing link and resends, with a confirmation warning. | developer-1 | agent/imran/admin-portal-ui`
- `2026-08-23T08:47:43Z | custom-module | Fixed monthly_rent silently reverting to the unit's rent on every save — removed the server-side fetch_from that was overriding it on every .save(), not just in Desk JS. | developer-1 | agent/imran/admin-portal-ui`
- `2026-08-23T08:52:20Z | payments-accounting | Fixed rent_schedule (drives invoicing and the PDF schedule table) staying stale after a term edit by adding rebuild_schedule_for_edited_terms, safe since only never-invoiced Sent leases can be edited. | developer-1 | agent/imran/admin-portal-ui`
- `2026-08-23T09:27:20Z | esign | OTP verification is now a 5-minute session gate instead of a one-time unlock — it re-requires OTP after the window lapses or from a different IP. | developer-1 | agent/imran/admin-portal-ui`
- `2026-08-23T09:43:45Z | esign | Fixed esign_envelope_id never being set by the real signing flow (only demo seeding set it) and added an admin-facing signed-contract download endpoint, replacing a dead-end 404. | developer-1 | agent/imran/admin-portal-ui`
- `2026-08-23T14:00:00Z | custom-module | Reset 5 demo leases to Draft (their fake e-sign documents had no real signed PDF behind them) and updated demo customer emails so e-sign test invites land in a catchable inbox. | developer-1 | main`
- `2026-08-23T14:30:00Z | multi-tenancy/ui-portal | Drafted ADRs for a separate Platform Control Plane site (tenant provisioning) and a renter self-service portal, both awaiting approval. | developer-1 | main`
- `2026-08-23T15:00:00Z | multi-tenancy/ui-portal | Approved ADR-0008 and ADR-0009; wrote implementation plans for tenant-provisioning-console, tenant-portal-ui, and maintenance-portal. | developer-1 | main`
- `2026-08-24T09:20:00Z | esign | Fixed a two-party signing showing no activity entry between "Sent" and "Signed by all parties" — each signer's signature is now also written to the Lease Agreement's activity feed. | developer-1 | main`
- `2026-08-24T05:15:00Z | ui-portal | Fixed / and /portal 404ing for a Tenant-only session — both routes now redirect to the first item in whichever nav the session actually has, instead of a hardcoded /overview. | developer-1 | main`
- `2026-08-24T06:00:00Z | ui-portal | Tenant detail page polish: portal-login toggle now reflects actual state, linked-record tables resolve Link columns to display labels, PDCs moved off the Customer page onto the Lease page, and activity now aggregates across all of a tenant's leases and maintenance requests. | developer-1 | main`
- `2026-08-24T04:30:00Z | ui-portal/compliance-security | Implemented ADR-0009: a read-only Tenant role scoped via User Permission on Customer, internal Maintenance Request fields hidden via permlevel, and portal APIs that never trust client-supplied identity; verified live with two real tenant logins seeing only their own records. | developer-1 | main`
- `2026-08-24T16:00:00Z | payments-accounting | Approved and implemented ADR-0010: Cheque Bank and Security Deposit DocTypes, PDC schedule generation/deposit/clear/bounce actions posting real Payment Entries, and a fix for an IDOR in get_linked_records that let a tenant read another tenant's PDCs. | developer-1 | main`
- `2026-08-24T16:30:00Z | payments-accounting | Fixed the New Lease dialog's deposit type/check/bank fields being invisible until an amount was typed with no hint they existed — the selector now always shows. | developer-1 | main`
- `2026-08-24T18:00:00Z | cross-cutting | Wrote an independent gap-analysis finding (ideal arbitrage-operator ERP vs. current code): the cost side of the business (head lease, landlord payables, cost centers) is largely unbuilt, no reports exist, RBAC has only 2 of 5 planned roles, and the React portal source lives only outside the repo. | developer-1 | main`
- `2026-08-24T19:00:00Z | esign/payments-accounting | Fixed a live incident where a lease was sent for e-signature with a stale fallback rent schedule before its real PDC schedule was generated — leases created for esign no longer auto-send, and sending is now blocked until a real schedule exists. | developer-1 | main`
- `2026-08-24T19:30:00Z | cross-cutting | Wrote draft ADRs for Head Lease/landlord-payables and lease-lifecycle-state, plus 5 new feature files, and refreshed IMPLEMENTATION-PLAN.md and HANDOFF.md to match actual shipped status. | developer-1 | branch: N/A (vault-only)`
- `2026-08-24T20:00:00Z | ui-portal | Fixed Structural Risk R1: moved the React portal source into the app repo (was only on one machine) and deleted the dead pre-ADR-0007 Vue portal tree; rebuilt output matched the previously committed bundle. | developer-1 | branch: agent/developer-1/portal-source-consolidation (PR #1, not yet merged — main-branch git ops blocked for this session, see PR)`
- `2026-08-24T20:15:00Z | payments-accounting | Implemented per-building Cost Centers: Building.after_insert now creates one and it's wired into the Sales Invoice item line, making per-building margin visible for the first time. | developer-1 | branch: agent/developer-1/cost-center-per-building (PR #2, not yet merged — main-branch git ops blocked for this session, see PR)`
- `2026-08-24T20:45:00Z | ui-portal/payments-accounting | Review round on two open PRs fixed: no real secrets in pushed scaffold files (cleaned up anyway), a Cost Center parent-group bug that would have broken the first Building, a backfill filter that matched zero rows due to NULL vs empty string, and safer error handling around Cost Center creation. | developer-1 | branch: agent/developer-1/portal-source-consolidation (PR #1), agent/developer-1/cost-center-per-building (PR #2) — neither merged yet`
- `2026-08-24T21:15:00Z | cross-cutting | Approved ADR-0011 (Head Lease 1:1 per Building, landlord payables via outgoing PDC with a configurable per-Building payment method) and ADR-0012 (3 distinct lease terminal states); numbered both and updated the gated feature files. | developer-1 | branch: N/A (vault-only)`
- `2026-08-24T21:45:00Z | custom-module | Implemented ADR-0012: a stored lease_status field (8 states, 3 distinct terminals) decoupled from esign_status, with a daily scheduler moving Active→Expiring Soon→Expired and a backfill patch for existing leases. | developer-1 | branch: agent/developer-1/lease-lifecycle-state (PR #3, not yet merged)`
- `2026-08-25T05:00:00Z | custom-module | Implemented Head Lease DocType (1:1 per Building) and Head Lease Schedule child table, plus Building.landlord_payment_method and auto-created Landlord.supplier records, as the first of 3 PRs for ADR-0011. | developer-1 | branch: agent/developer-1/head-lease-doctype (PR #6, not yet merged)`
- `2026-08-25T05:30:00Z | payments-accounting | Generalized PDC Entry with a direction field (Incoming/Outgoing) and head_lease/purchase_invoice links so the same reconciliation code handles landlord cheques, fixing a dashboard aggregate that would have blended landlord and tenant cheques together. | developer-1 | branch: agent/developer-1/pdc-direction (PR #7, not yet merged)`
- `2026-08-25T05:35:00Z | payments-accounting | Fixed get_accounts_data throwing on every call since the original portal commit — a string vs. date-object bug in the revenue-by-month calculation; the Accounts page revenue chart had never actually worked until this fix. | developer-1 | branch: agent/developer-1/fix-accounts-revenue-dates (PR #8, merged and deployed)`
- `2026-08-25T06:00:00Z | payments-accounting | Implemented landlord payables: build_payment_schedule auto-generates a Head Lease's amortization schedule, create_payable_for_head_lease posts a submitted Purchase Invoice with the Building's cost center, and a daily scheduler handles the Bank-Transfer path. | developer-1 | branch: agent/developer-1/landlord-payables (PR #9)`
- `2026-08-25T06:45:00Z | payments-accounting | Deployed and verified landlord payables end-to-end on production: a real Head Lease correctly generated its schedule and posted/reconciled 3 Purchase Invoices, all test data cleaned up afterward. | developer-1 | branch: agent/developer-1/landlord-payables (PR #9, merged and deployed)`
- `2026-08-25T07:00:00Z | payments-accounting | Added an outgoing-PDC generator for PDC-path Buildings and made cheque clearance post the Purchase Invoice at that moment, closing the gap where landlord payables had no PDC path; also fixed a double-billing risk when a Building's payment method changes after cheques are issued. | developer-1 | branch: agent/developer-1/outgoing-pdc-generator (PR #10, merged and deployed)`
- `2026-08-25T07:20:00Z | payments-accounting | Fixed a confirmed production data-exposure bug: PDC Entry's tenant read permission didn't exclude landlord (Outgoing) rows, letting a tenant read another landlord's cheque data; fixed with permission hooks and verified live. | developer-1 | branch: agent/developer-1/pdc-entry-tenant-scoping (PR #11, merged and deployed)`
- `2026-08-25T15:20:00Z | ui-portal | Built the Head Lease admin-portal UI (payment schedule panel, generate-cheques action, deposited-cheque confirmation), the only way to reach the ADR-0011 backend outside of bench console until now. | developer-1 | branch: agent/developer-1/head-lease-ui (PR #12, merged and deployed)`
- `2026-08-25T16:45:00Z | custom-module | Fixed New Building's Landlord dropdown having no inline-create option, and made Amenities a business-configurable checkbox list instead of free text. | developer-1 | branch: agent/developer-1/building-form-fixes (PR #13, merged and deployed)`
- `2026-08-25T17:20:00Z | custom-module | Fixed Total Units not auto-creating units and choosing a Landlord not prompting a Head Lease — a landlorded Building can no longer get new Units until it has a Head Lease (existing pre-ADR-0011 Buildings/Units left untouched). | developer-1 | branch: agent/developer-1/building-setup-gating (PR #14, merged and deployed)`
- `2026-08-26T03:50:00Z | payments-accounting | Added a Revenue vs. Expense chart toggle to the Accounts page and fixed a bug where landlord payment entries could bleed into the tenant-collection KPI; also cancelled 3 stale demo leases found while verifying real PDC figures. | developer-1 | branch: agent/developer-1/accounts-income-expense (PR #15, merged and deployed)`
- `2026-08-26T04:10:00Z | ui-portal | Fixed Draft/Sent contracts having no Cancel action in the UI, and the Contracts table showing raw Customer/Unit IDs instead of names. | developer-1 | branch: agent/developer-1/cancel-draft-lease (PR #16, merged and deployed)`
- `2026-08-26T06:15:00Z | ui-portal | Added sortable columns, column filters, real search, and pagination to every list page, and fixed Contracts showing the frozen esign_status instead of the real lease_status. | developer-1 | branch: agent/developer-1/list-table-upgrade (PR #19, merged and deployed)`
- `2026-08-26T06:00:00Z | ui-portal | Extended the "<Building> - <Unit>" combined display from Contracts to the Maintenance list's Unit column. | developer-1 | branch: agent/developer-1/maintenance-unit-combined (PR #18, merged and deployed)`
- `2026-08-26T05:30:00Z | ui-portal | Fixed raw IDs showing instead of names on /maintenance and /properties, and made the Contracts Unit column show "<Building> - <Unit>" via a two-hop client-side join. | developer-1 | branch: agent/developer-1/portal-list-column-fixes (PR #17, merged and deployed)`
- `2026-08-26T09:30:00Z | payments-accounting | Built a per-building profitability report (income minus landlord cost minus maintenance cost, plus occupancy) and surfaced that most historical invoices predate their building getting a cost center, so a chunk of income is still unattributed. | developer-1 | branch: agent/developer-1/building-profitability-report (PR #20, merged and deployed)`
- `2026-08-27T05:00:00Z | custom-module | Fixed the Landlord edit form's Bank Name field lacking the same bank-picker/inline-create behavior as New Landlord, and started surfacing each field's DocType description and read-only state in the generic detail view. | developer-1 | branch: agent/developer-1/landlord-edit-bank-and-descriptions (PR #24, merged and deployed)`
- `2026-08-27T04:00:00Z | custom-module | Changed Landlord.bank_name from free text to a Link on Cheque Bank, matching every other bank reference in the app, with an inline-create picker. | developer-1 | branch: agent/developer-1/landlord-bank-link (PR #23, merged and deployed)`
- `2026-08-27T03:00:00Z | custom-module | Fixed the /landlords page's Add button doing nothing — added a New Landlord dialog and enabled Landlord in the create-allowed doctype list. | developer-1 | branch: agent/developer-1/landlord-add-button (PR #22, merged and deployed)`
- `2026-08-27T02:00:00Z | custom-module | Added a formal "Own Building" landlord placeholder and made Building.landlord mandatory, after a Building was accidentally saved with no landlord when a typed name wasn't submitted via the right button. | developer-1 | branch: agent/developer-1/own-building-landlord (PR #21, merged and deployed)`
- `2026-08-27T00:50:00Z | infra-ops | Wiped all real-estate transactional data (buildings, units, leases, invoices, etc.) at Imran's request to start fresh, after taking a full backup; preserved Company, Chart of Accounts, Users, and settings. | developer-1 | operation: data wipe (no PR — no code changed, direct production data operation via bench console, backed up first)`
- `2026-08-27T06:00:00Z | custom-module | Investigated a Customer/Tenant split and CRM request; drafted ADR-0013 recommending ERPNext's native Lead doctype for the pre-tenant pipeline and curating the Customer edit form's field set, approved with a hard "never show ERPNext" constraint. | developer-1 | ADR: 0013-lead-crm-and-tenant-field-curation`
- `2026-08-27T06:15:00Z | custom-module | Implemented Customer field curation (Portal Field Visibility now applies to edit mode too) and fixed Tab Break fields leaking into that config. | developer-1 | branch: agent/developer-1/tenant-field-curation (PR #25), agent/developer-1/tab-break-field-skip (PR #26), both merged and deployed; live Portal Field Visibility row corrected via bench console`
- `2026-08-27T07:00:00Z | payments-accounting | Fixed PDC schedule regeneration always failing after the first generation — it deleted PDC Entries before clearing the Rent Schedule rows that still linked to them; reordered to fix. | developer-1 | branch: agent/developer-1/pdc-regenerate-link-fix (PR #27, merged and deployed)`
- `2026-08-27T08:00:00Z | payments-accounting | Locked PDC Entry term fields once its lease is Signed, and curated PDC Entry's visible fields down to the ones a business owner actually needs. | developer-1 | branch: agent/developer-1/pdc-lock-and-field-curation (PR #28, merged and deployed)`
- `2026-08-27T09:00:00Z | ui-portal | Cleaned up the contract detail page: removed the redundant locked Payment Schedule panel once cheques exist, renamed "Signing" to "Contract", and removed the dead-end E-Sign Document link. | developer-1 | branch: agent/developer-1/contract-page-cleanup (PR #29, merged and deployed)`
- `2026-08-27T10:00:00Z | payments-accounting | Added a This-Month/Next-Month cash flow projection panel, since the existing Revenue/Expense chart only shows trailing actuals and a new portfolio has little history yet. | developer-1 | branch: agent/developer-1/cash-flow-projection (PR #30, merged and deployed)`
- `2026-08-27T11:00:00Z | payments-accounting | Fixed a lease's first rent invoice posting immediately at signing regardless of whether the period was actually due, which could bill a tenant nearly two weeks early; now gated on the same due-date check the scheduler uses. | developer-1 | branch: agent/developer-1/fix-early-invoice-on-signing (PR #31, merged and deployed)`
- `2026-08-27T11:30:00Z | payments-accounting | Cancelled one wrongly early-dated invoice created before the PR #31 fix and reset its schedule row to Pending, correcting August revenue to the right figure. | developer-1 | operation: data correction (no PR — no code changed, direct production data fix via bench console, confirmed with Imran first)`
- `2026-08-27T12:00:00Z | payments-accounting | Implemented ADR-0014: replaced the automatic Pending→Deposited sweep with a manual staff action, and replaced the Accounts page's PDC-pending stat with a more actionable "Cheques past due" figure. | developer-1 | ADR: 0014-manual-bank-deposit-workflow; branch: agent/developer-1/manual-bank-deposit (PR #32, merged and deployed)`
- `2026-08-27T13:00:00Z | payments-accounting | Investigated a full accounting system (P&L, Balance Sheet, Journal Entry, GL): confirmed ERPNext's own report engines already work correctly against real data, but default account names are generic and no owner's-equity entry has been posted; wrote a finding and a draft ADR recommending portal pages that call ERPNext's existing reports. | developer-1 | finding: 2026-08-27-full-accounting-system.md; draft ADR: draft-adr-portal-native-accounting-reports.md (awaiting review)`
- `2026-08-27T14:00:00Z | multi-tenancy | Answered whether renaming accounts for real estate risks affecting a future module: no, tenant-per-site already isolates each client's Chart of Accounts; drafted an ADR adding a module field to tenant provisioning and letting each vertical app own its own CoA setup. | developer-1 | draft ADR: draft-adr-module-aware-provisioning.md (awaiting review, supersedes/refines 0008 pending approval)`
- `2026-08-27T15:00:00Z | multi-tenancy | Investigated platform modularization (domain OS apps, reusable modules, extension tiers): found e-sign's provider-abstraction field was never actually wired up; wrote a finding and a draft ADR proposing hooks-based module dispatch, with finishing e-sign as the first real implementation. | developer-1 | finding: 2026-08-27-platform-modularization.md; draft ADR: draft-adr-platform-modularization.md (awaiting review)`
- `2026-08-28T08:00:00Z | payments-accounting | Shipped ADR-0015 phase 1: General Ledger + Trial Balance portal viewers calling ERPNext's own report engines directly, plus a patch renaming the default income/expense accounts to real-estate labels (Sales -> Rental Income, Cost of Goods Sold -> Landlord Rent Expense) via ERPNext's own account-rename entry point. | developer-1 | branch: agent/developer-1/accounting-reports (PR #33, merged and deployed)`
- `2026-08-28T08:05:00Z | payments-accounting | Posted the one-time opening Journal Entry for owner's capital (100,000 QAR, 2026-07-01, marked is_opening) so the Balance Sheet/Trial Balance read correctly from day one; confirmed with Imran first. | developer-1 | operation: data correction (no PR — direct production data entry via bench console)`
- `2026-08-28T09:00:00Z | compliance-security | Investigated RBAC before drafting an ADR: found the existing feature file's 4-role model conflicted with the findings doc's 5-role model, and that only 1 real staff account exists today; wrote a draft ADR reconciling both and proposing Cost Center-based Maintenance Staff scoping. | developer-1 | draft ADR: draft-adr-role-based-access.md (approved same day as ADR-0018)`
- `2026-08-28T09:30:00Z | compliance-security | Shipped ADR-0018: Leasing Agent, Accountant, and Maintenance Staff roles with a full DocPerm permission matrix, plus Cost Center-based building scoping for Maintenance Staff hardened against the PDC Entry IDOR class of bug. | developer-1 | branch: agent/developer-1/role-based-access (PR #34, merged and deployed)`
- `2026-08-28T09:35:00Z | custom-module | Fixed Maintenance Request.cost_center never being auto-populated (only demo_data.py's seeding ever set it); added Unit -> Building -> Cost Center resolution in validate(), found while implementing Maintenance Staff's building-scoping. | developer-1 | branch: agent/developer-1/role-based-access (PR #34)`
- `2026-08-28T19:00:00Z | ui-portal | Fixed /reports rendering AccountsView instead of ReportsView: both nav items share the same api string, and a substring check on it matched both, so Reports never reached its own branch. Switched route dispatch to match on nav-item label instead. | developer-1 | branch: agent/developer-1/fix-reports-route (PR #35, merged and deployed)`
- `2026-08-28T19:20:00Z | payments-accounting | Curated the General Ledger view from 15 columns to 9 and added an inline note explaining that the all-accounts Total/Closing row always shows equal debit=credit by design; verified the 8,000 figure directly against ERPNext's own report engine (not a bug). | developer-1 | branch: agent/developer-1/gl-columns (PR #36, merged and deployed)`
- `2026-08-28T19:30:00Z | payments-accounting | Shipped ADR-0015 phase 2: Profit & Loss and Balance Sheet viewers calling ERPNext's own report engines; confirmed live that the cost_center filter gives a correct per-building P&L. Journal Entry UI (now unblocked by RBAC) is the only remaining piece of this feature. | developer-1 | branch: agent/developer-1/pl-balance-sheet (PR #37, merged and deployed)`
- `2026-08-29T03:20:00Z | payments-accounting | Fixed mark_pdc_cleared silently failing to reconcile Incoming PDCs: PDC Entry.sales_invoice was never written back by invoice generation (only the Rent Schedule row's own copy was), so a Cleared cheque left its invoice permanently Overdue with no error. Fixed both invoicing.py (writes the link forward) and pdc.py (self-heals via the schedule row). | developer-1 | branch: agent/developer-1/fix-pdc-invoice-link (PR #38, merged and deployed)`
- `2026-08-29T03:30:00Z | payments-accounting | Backfilled the one live record the reconciliation bug had hit (PDC-00342/ACC-SINV-2026-00032) using the fixed mark_pdc_cleared path itself; confirmed with Imran first. Invoice now Paid, new Payment Entry ACC-PAY-2026-00002 posted, P&L/Balance Sheet totals unaffected as expected. | developer-1 | operation: data correction (no PR — direct production data fix via bench console)`
- `2026-08-29T04:05:00Z | payments-accounting | Stripped the pointless " - RRE" company-abbreviation suffix from account names in GL/TB/P&L/BS report tables (display only, scoped to columns typed as Account links); every tenant is its own single-Company site so the suffix never disambiguates anything here. | developer-1 | branch: agent/developer-1/account-display-name (PR #39, merged and deployed)`
- `2026-08-29T04:45:00Z | payments-accounting | Shipped step 1 of company bank account setup: real GL-linked Bank Account records (Settings panel), deliberately separate from the tenant-managed Cheque Bank label doctype; the first account created becomes Company.default_bank_account. | developer-1 | branch: agent/developer-1/bank-account-setup (PR #40, merged and deployed)`
- `2026-08-29T04:52:00Z | payments-accounting | Fixed PR #40 same-day: Company.default_bank_account is a Link to the GL Account, not to the Bank Account record — the first cut set it to the wrong doctype's name, which silently kept every payment falling back to Cash. Caught during live post-deploy verification. | developer-1 | branch: agent/developer-1/bank-account-default-fix (PR #41, merged and deployed)`
- `2026-08-29T05:20:00Z | payments-accounting | Made the "Add bank account" Bank field a picker with inline "+ Add bank" instead of free text, matching every other bank field in the app. | developer-1 | branch: agent/developer-1/bank-account-bank-picker (PR #42, merged and deployed)`
- `2026-08-29T05:35:00Z | payments-accounting | Fixed PR #42 same-day: the new picker pointed at ERPNext's core Bank doctype (empty) instead of the app's one existing shared Cheque Bank list (already had QNB/Commercial Bank) — Imran caught it immediately since the dropdown was empty. Reverted to the shared list; the core Bank record is now purely invisible plumbing auto-synced underneath, same pattern as Landlord.supplier. | developer-1 | branch: agent/developer-1/shared-bank-list (PR #43, merged and deployed)`
- `2026-08-29T06:00:00Z | payments-accounting | Shipped step 3 of bank-account-setup: Head Lease.bank_account for outgoing landlord cheques, auto-selected at creation when the company has one bank account, resolved into a real GL account at PDC clearance instead of the implicit Cash fallback; throws a clear "add a bank account first" error if none can be resolved. Closes the flagged outgoing-cheque-bank gap in landlord-payables.md. | developer-1 | branch: agent/developer-1/outgoing-bank-account (PR #44, merged and deployed)`
- `2026-08-29T06:10:00Z | payments-accounting | Backfilled HL-2026-00368.bank_account (the head lease Imran flagged) to the company's one bank account, since the field didn't exist when it was created; its 12 remaining Pending PDCs now resolve correctly. Its one already-Cleared PDC's Payment Entry stays posted to Cash — submitted, can't be edited; Imran's call was to leave it, not post a correcting entry. | developer-1 | operation: data correction (no PR — direct production data fix via bench console)`
- `2026-08-29T06:30:00Z | payments-accounting | Shipped the final step of bank-account-setup: incoming rent deposits now record which company bank account received them (PDC Entry.bank_account, an already-existing but never-wired field), auto-selected with one account, a required picker once more than one exists. Marks the feature done — all payments now resolve to a real bank account instead of Cash by default. | developer-1 | branch: agent/developer-1/incoming-deposit-account (PR #45, merged and deployed)`
- `2026-08-29T06:50:00Z | payments-accounting | Shipped the last piece of accounting-reports (ADR-0015): a portal Journal Entry create/list UI, draft-then-submit rather than insert-and-submit so a manual GL posting always has an undo path. Also granted Accountant explicit submit/cancel rights on Journal Entry, which the original RBAC grant had missed. Verified live end-to-end (create, submit, appears in GL, cancel, delete, books unaffected) through the actual API; marks accounting-reports fully done. | developer-1 | branch: agent/developer-1/journal-entry-ui (PR #46, merged and deployed)`
- `2026-08-29T07:15:00Z | esign | Wired real esign_mode dispatch (ADR-0017 first slice): the field has existed since ADR-0002 but was read nowhere — both call sites imported the in-built implementation directly. New e_sign/dispatch.py resolves it via frappe.get_hooks("esign_provider"), throwing cleanly if a selected mode has no registered provider. Documented the ESignProvider contract in the vault. Physical extraction into its own module app and the Zoho Sign provider itself are deliberately deferred — new-bench-app-on-production risk, and blocked on Imran providing Zoho credentials, respectively. | developer-1 | branch: agent/developer-1/esign-dispatch (PR #47, merged and deployed)`
- `2026-08-29T20:15:00Z | esign | Extracted in-built e-sign into its own app, rafaii/inbuilt-esign, per Imran's go-ahead ("every module's audit trail sits in the respective sites" — confirmed correct live). Took a full DB backup first. Found and fixed two real couplings mid-extraction (add_activity, and Lease Agreement-specific finalize logic — now a generic doc_events dispatch via doc.run_method) plus a third dead-dispatch call site neither this nor PR #47 had caught (a Desk Client Script). Verified live: existing E-Sign Document/audit-log records intact, dispatch resolves to the new app, /esign still serves. | developer-1 | repo: rafaii/inbuilt-esign; branch: agent/developer-1/esign-extraction (real-estate PR #48, merged and deployed)`
- `2026-08-30T00:10:00Z | payments-accounting | Extracted portal-native accounting (GL/Trial Balance/P&L/Balance Sheet, Journal Entry, Bank Account setup) into its own app, rafaii/accounts-portal, per Imran's request to also modularize "the accounts module" — planned and approved first (vault/decisions/draft-adr-accounts-portal-extraction.md), grounded in a repo-wide survey. Unlike e-sign, this app owns zero custom DocTypes, so no DocType module reassignment was needed. | developer-1 | repo: rafaii/accounts-portal (private); branch: agent/developer-1/accounts-portal-extraction (real-estate PR #49, merged and deployed)`
- `2026-08-30T00:27:00Z | payments-accounting | Fixed PR #49 same-day: every accounts_portal dotted path was wrongly double-nested (copied inbuilt_esign's three-level pattern without noticing accounts_portal has no module folder since it owns no DocTypes) — broke the live Reports page, Journal Entry panel, and Bank Accounts settings panel until caught by post-deploy verification. Also made all new repos private by default going forward (Imran's explicit preference), retroactively switching rafaii/inbuilt-esign from public to private, which required adding VPS git auth for both private clones. | developer-1 | branch: agent/developer-1/accounts-portal-pathfix (real-estate PR #50, merged and deployed)`
- `2026-08-30T00:50:00Z | ui-portal | Added a "Modules & integrations" panel to Settings so Imran can see what's installed on the site: extension apps (real_estate_os/inbuilt_esign/accounts_portal, via Frappe's own get_versions()) and the active e-sign provider, with a live check that something is actually registered for it rather than just echoing the configured mode. Verified against the actual deployed endpoint before and after deploy. | developer-1 | branch: agent/developer-1/modules-integrations-settings (PR #51, merged and deployed)`
- `2026-08-30T01:35:00Z | ui-portal | Fixed PR #51 same-day: Imran caught inbuilt_esign listed as both a generic Module and the resolved E-Sign Integration - same fact twice, would have doubled up again with a future Zoho provider. Modules now excludes any app registered as a provider for a swappable integration slot; Integrations resolves the provider's own app title instead of echoing the raw esign_mode string. | developer-1 | branch: agent/developer-1/modules-provider-fix (PR #52, merged and deployed)`
- `2026-08-30T03:00:00Z | payments-accounting | Read vault/expert-suggestions/accounting-ui-wireframe.md and wrote a full gap analysis against the actual portal code (fact-checked via a fork, not from memory): strong on vertical-to-GL architecture and per-building P&L, missing GL drill-down/Party filter/Chart of Accounts/AR-AP aging/Bank Reconciliation. Imran corrected the initial framing of Bank Reconciliation as optional - not all tenants pay via PDC, some e-transfer, so it's a must-have. | developer-1 | doc: vault/payments-accounting/accounting-ui-gap-analysis.md`
- `2026-08-30T04:00:00Z | payments-accounting | Shipped gap-analysis Phase 1 (GL drill-down via a new read-only VoucherPreviewDialog, Party filter on GL, "View Accounting Impact" panels on Lease Agreement/Head Lease, Journal Entry audit info) and Phase 5b Part A (Lease Agreement.payment_method models Bank Transfer rent collection alongside PDC, closing the gap Imran flagged - generate_due_invoices needed no change since it already worked generically off rent_schedule). Both verified live end-to-end. | developer-1 | branches: agent/developer-1/phase1-traceability (PR #53), agent/developer-1/bank-transfer-rent-schedule (PR #54); accounts-portal@4985d03 — all merged and deployed`
- `2026-08-30T07:45:00Z | payments-accounting | Shipped gap-analysis Phase 5b Part B: Bank Reconciliation UI (BankReconciliationPanel on Reports page) wrapping a new accounts_portal.bank_reconciliation module. Found live that ERPNext's own get_linked_payments/create_payment_entry_bts only match invoices already paid through that specific bank account (POS pattern), not our Outstanding-then-settled-later invoices, so wrote a direct open-invoice query and used get_payment_entry + reconcile_vouchers instead. Verified end-to-end against a throwaway invoice, cleaned up with no residue. Closes the reconciliation gap for both e-transfer rent and never-reconciled landlord Bank-Transfer payables. | developer-1 | branch: agent/developer-1/bank-reconciliation-ui (real-estate PR #55, merged and deployed); accounts-portal@c6632d9`
- `2026-08-30T08:15:00Z | payments-accounting | Fixed same-day, caught by advisor review before reporting Phase 5b Part B done: match_transaction_to_invoice called get_payment_entry with no party_amount, which builds the Payment Entry for an invoice's full outstanding regardless of the actual bank line amount - a bank line smaller than the invoice (deposit 3000 vs. outstanding 5000) wrongly submitted a Payment Entry for 5000 and marked the invoice fully Paid. The first live test only used an exact-amount match, which couldn't expose this. Fixed by passing settle_amount = min(bank line unallocated, invoice outstanding) as party_amount; also fixed get_matching_invoices only ranking the 100 most-recently-posted invoices before sorting by amount closeness. Re-verified live: partial payment now correctly leaves the invoice Partly Paid at the right remaining balance. | developer-1 | accounts-portal@636aa56 (direct to main, no PR - matches this app's existing no-worktree pattern); redeployed`
- `2026-08-30T09:10:00Z | ui-portal | Imran pointed at saas-admin-ui-main's Accounts page as an easy-navigation model; rebuilt the Reports page's navigation to match - a persistent top tab bar (Overview/GL/Trial Balance/P&L/Balance Sheet/Journal Entries/Bank Reconciliation) instead of a card grid you had to back out of before opening a different report. Also dropped three dead placeholder cards that had no onClick. Mid-implementation Imran corrected the scope - he initially asked to merge Accounts into this page, then clarified Reports is for in-depth reporting and Accounts should stay separate as the humanized, current-period view for a non-accounting owner - so the two stayed distinct sidebar items, only Reports' internal navigation changed. | developer-1 | branch: agent/developer-1/reports-tab-navigation (PR #56, merged and deployed)`
- `2026-08-30T10:30:00Z | ui-portal/payments-accounting | Imran said the plain Accounts ops dashboard had no value - removed it, renamed Reports to take over the /accounts route/label (dropping its Real-Estate-specific PDC Overview tab), added a new /actions page for the relocated PDC action queues (his call: dashboard shows counts + a link, bulk actions get their own page), and rebuilt /overview per vault/expert-suggestions/overview-ui-wireframe.md as a plain-language, spread-focused dashboard (money collected minus fixed Head Lease rent minus maintenance = the spread) reusing get_building_profitability rather than building new per-unit allocation. Advisor review before deploy caught a real blocker: get_owner_overview called accounts_portal's get_bank_accounts(), which hard-throws PermissionError for any role without Bank Account read - confirmed live that Leasing Agent and Maintenance Staff both lack it, which would have broken their landing page. Fixed by querying Bank Account inline and passing ignore_account_permission to get_balance_on. Also fixed on the same pass: the trend chart and hero card both being labeled "profit" while computing different things (cash-basis-with-maintenance vs. accrual-without), the overdue-tenants list missing genuinely-late invoices because it only matched status=Overdue (which flips on a nightly job, not on due_date), a "paid on time" label on a count that only checked status=Paid, and an unbounded renewals query that would surface master-lease renewals up to a year out under "what's coming up." Two self-caught issues during implementation: a two-hop dotted-field fetch that Frappe silently drops, and nearly deleting CashFlowProjectionPanel as dead code before checking it backs a tracked in-progress feature (cash-flow-forecast.md) - restored and relocated into Overview instead. | developer-1 | branch: agent/developer-1/owner-overview-accounts-actions`
- `2026-08-31T00:00:00Z | ui-portal | Wrote vault/ui-portal/all-page-ui-gap-analysis.md: fact-checked vault/expert-suggestions/all-page-ui-wireframe.md (an expert's review of Properties/Landlords/Contracts/Tenants) against the actual code via a fork, not from memory. Two of the wireframe's "structural gaps" were misdiagnosed - Head Lease has been its own DocType since ADR-0011 and Unit already exists with full occupancy data, both just missing a top-level sidebar item, not a data-model conflation. Also root-caused two real bugs beyond the wireframe's own guess: the Lease Agreement page's two-different-IDs bug (title_field: customer showing the Customer Link value) and the Landlords contact-completeness KPI (checks email_id, but Landlord's field is named email and isn't even fetched). | developer-1 | doc: vault/ui-portal/all-page-ui-gap-analysis.md`
- `2026-08-31T04:37:00Z | ui-portal | Imran: "Not sure if we need to add new sidebar items. Lesser the better. Under contracts page, we can create 2 tabs or sections - one for tenant and one for landlord lease." Split Contracts into Tenant Leases / Landlord Leases tabs entirely client-side (no new portal_nav_items entry, no new backend endpoint) - Landlord Leases (Head Lease) is browse-only since it has no standalone create flow, always created from a Building's "Set up Head Lease" dialog. Confirmed live that the two staff roles who see Contracts today (Leasing Agent, Accountant) both already have Head Lease read permission, and that Tenant-only sessions use a separate nav config that never reaches this tab bar at all. Advisor review caught head_lease_status added to the metrics/column-label allowlists but not renderCell's status-badge list, which would have rendered Head Lease status as plain text instead of a badge - fixed before shipping. Units left exactly as-is per Imran's answer to a follow-up question (no portfolio-wide list for now). | developer-1 | branch: agent/developer-1/contracts-lease-tabs (PR #58, merged and deployed)`
- `2026-08-31T16:27:00Z | ui-portal | Continued the gap-analysis's phased plan (Phase 1 remainder + all of Phase 2): added a Head Leases tab to the Landlord detail page (previously an extra hop through Building), fixed the Landlords "With contact" metric checking the wrong field name (email_id instead of email) and not even fetching it, fixed PDC lists rendering out of check-date order (no order_by, fell back to edit order), added a server-side computed "Overdue" status for past-due Pending PDC rows (scoped exactly to PDC Entry, using the server's today() - not a frontend heuristic on the browser's clock, which an earlier draft used until advisor review flagged both that and an unrelated-doctype collision risk), and fixed the Lease Agreement page showing two different IDs for the same record by dropping title_field: "customer" so it falls back to its own LSE- name. Required bench migrate for the doctype JSON change; verified live post-migrate that title_field actually cleared to None rather than assuming the JSON edit alone was enough. | developer-1 | branch: agent/developer-1/landlord-tabs-and-fixes (PR #59, merged and deployed)`
- `2026-08-31T21:21:00Z | payments-accounting | Imran picked Rent Roll & Arrears next from the P1 list. Its 2026-08-24 design called for a Frappe Script Report (Desk-only) - predated every other report built since, which all shipped as portal Accounts tabs instead - so per Imran's answer to a clarifying question, it shipped the same way: a new "Rent Roll" tab, not a Script Report or a new sidebar item. New get_rent_roll(): one row per active lease with balance/days-overdue/aging bucket (Current/0-30/31-60/61-90/90+), plus a collection-rate summary per building and portfolio-wide. Advisor review caught a real bug before deploy: the first draft summed ALL submitted invoices into "billed" with no due-date bound, so a tenant fully paid-to-date but with their next period's invoice already posted showed a 50% collection rate purely from money that wasn't owed yet. Fixed by scoping billed/outstanding to invoices that have actually come due; corrected the feature file's own acceptance criterion, which wrongly demanded this tie out to get_accounts_data's all-time Payment-Entry totals - a different, deliberately non-matching number. Verified live across an actual day-boundary crossing during testing (server's today() advanced from 2026-08-31 to 2026-09-01 between two checks), confirming the aging logic responds to real date changes rather than a static snapshot. Also transferred the ui-portal gap-analysis's remaining Phase 4 work and this feature into IMPLEMENTATION-PLAN.md for tracking, per Imran's request. | developer-1 | branch: agent/developer-1/rent-roll-and-arrears (PR #60, merged and deployed)`
- `2026-08-31T23:00:00Z | payments-accounting | Imran picked Cash Flow Forecast v2 next. get_cash_flow_projection now takes a configurable months param (default 6, clamped 1-24) returning a months array with a running balance and a crunch_month flag, instead of the v1-hardcoded this_month/next_month pair - running balance starts from today's actual cash on hand (same inline Bank Account query as get_owner_overview) so a flagged shortfall reads as a real projected balance date, the "break-even indicator" the original plan called for. Overview's existing compact CashFlowProjectionPanel now reads months[0]/months[1] from the same richer shape with no visual change; new "Cash Flow" tab on Accounts (CashFlowForecastPanel) shows the full horizon with a 3/6/12-month selector. Advisor review flagged that this site's recurring QAR 0 cash-on-hand figure (no bank feed; historical PDC clearances post to Cash) would make the crunch flag fire on the very first month every time, reading as a false alarm rather than the bookkeeping artifact it is - added an explicit caveat banner for that case instead of suppressing the flag. Also clamped the months param (a whitelisted endpoint reachable by any logged-in user could otherwise request 600 months / 1200 unbounded SQL queries) and grepped both apps for stale callers of the old this_month/next_month keys before deploying - none found. | developer-1 | branch: agent/developer-1/cash-flow-forecast-v2 (PR #61, merged and deployed)`
- `2026-08-31T23:40:00Z | ui-portal | Imran reported: "Clicking on the 'View 6-month forecast' opens the /accounts page but not on the Cash Flow tab." AccountsView's tab state always initialized to "gl" with no way to specify a starting tab via a link - the button just called navigate("/accounts"). Added ?tab=... query-string support: an isAccountTab() type guard validates against the real ACCOUNT_TABS keys, initial state reads it once on mount via useLocation().search, and the "View 6-month forecast" button now navigates to /accounts?tab=cashflow. | developer-1 | branch: fix/accounts-tab-deep-link (PR #62, merged and deployed)`
- `2026-08-31T23:55:00Z | ui-portal | Imran reported the follow-up bug: "now it works with tab=cashflow when clicking on 6-month forecast. But if I change the tabs on that page, the url still stays on tab=cashflow." PR #62's ?tab= fix only handled the initial load - clicking a different tab afterward updated in-memory state but never wrote back to the URL. Added a selectTab() wrapper that calls both setTab() and navigate(`/accounts?tab=${key}`, { replace: true }) (replace, not push, so tab-clicking doesn't pile up browser-history entries), and switched every tab button's onClick to use it. | developer-1 | branch: fix/accounts-tab-url-sync (PR #63, merged and deployed)`
- `2026-08-31T02:15:00Z | esign | Continued down IMPLEMENTATION-PLAN.md's P1 list to the one remaining unblocked item: provider-abstraction.md's "Business Config page: esign_mode selector" line - the dispatch (e_sign/dispatch.py) has read esign_mode dynamically since PR #47, but changing it was still Desk-only. get_settings_data() now returns esign.mode/esign.modes (each option flagged active via the same has-a-registered-provider check get_integrations() already does against the esign_provider hook); new set_esign_mode(mode) validates against the DocType's real Select options and saves to the Signature Settings singleton, mirroring set_business_signatory. Settings page gained a matching "E-sign provider" selector with a warning banner when the saved mode has no registered provider. Advisor review caught a real authorization gap before deploy: Signature Settings write is System Manager-only (no Custom DocPerm rows) but the Settings page renders for every role, so both this new selector and the pre-existing signatory selector would let a non-admin fill the form and hit a raw PermissionError on Save - fixed by adding esign.can_manage (frappe.has_permission, no throw) and disabling both controls in the UI when false. Verified live pre- and post-deploy via bench console: mode/modes/can_manage round-trip correctly, an invalid mode is rejected, a full save/restore cycle persists, and can_manage reads True for Administrator / False for Guest. Not click-tested in a browser - flagged explicitly given the prior two entries above were exactly this failure mode. | developer-1 | branch: feat/esign-mode-settings-ui (PR #64, merged and deployed)`
- `2026-09-01T00:00:00Z | maintenance | Imran picked maintenance-cost-allocation next from P2. Its 2026-08-16 plan called for GL posting (Expense Claim/Stock Entry) on resolve - per Imran's answer to a scope question, deferred: get_building_profitability()/get_owner_overview() already read Maintenance Request.total_cost directly with an existing comment noting this is deliberate ("real cost data, just not GL-sourced"), and GL posting would need Warehouse/Employee master data this app doesn't otherwise use. The real gap fixed instead: total_cost only ever summed spares_used - labor_hours existed as a field but was never priced into anything. Added Real Estate Settings.default_labor_rate (site-wide) and Maintenance Request.labor_rate/labor_cost (per-request override), same per-row-override shape as Maintenance Spare.rate. Found and fixed a real data leak along the way: get_doc_detail (the generic detail-page endpoint) never filtered fields by DocField permlevel, so Maintenance Request's internal cost fields were already exposed to a Tenant viewing their own request - adding labor_rate at the same permlevel would have leaked the actual hourly cost basis. Fixed generically via meta.get_permlevel_access, confirmed (by grepping every doctype hooks.py reaches through get_doc_detail) this is the only doctype affected, and verified live end-to-end with a scratch Tenant test user before and after deploy. bench migrate run for two DocType JSON changes; zero existing Maintenance Request records on this site, so no backfill needed. Caveat left in the feature file: default_labor_rate is unset on the live site, so total_cost stays spares-only until a System Manager sets it. | developer-1 | branch: feat/maintenance-labor-cost (PR #65, merged and deployed)`
- `2026-09-01T02:00:00Z | multi-tenancy | Imran: "Let us create console.nnuggets.com - that is used to onboard new customers which also lets Saas admin decide what OS they are and what all integration they need." Then, mid-plan-review: "console.nnuggets.com... is going to be the main backend of our Saas that see all businesses using our product, their subscription etc. For now, we are only building the onboarding of new customers." Built ADR-0008/0016's tenant-provisioning-console, unbuilt until now. New private repo rafaii/platform-console (platform_console app), installed only on the new console.nnuggets.com site - never on a tenant site, preserving the isolation those ADRs were written for. Tenant Site form (Platform Admin only) creates a real site, installs the chosen module (Real Estate only today - MODULE_APP_MAP is one entry per module, no separate mapping DocType per ADR-0016), sets up its Company with a per-country Chart of Accounts template (Imran's own refinement mid-review: "There may be customers from Canada, US, UK, India, Saudi etc. So not everyone uses the same CoA" - wraps ERPNext's own get_charts_for_country, the same function Company's Desk form uses), and writes its Traefik route, all via a background job (bench new-site can exceed a request timeout) polled from the form. Entered plan mode given the scale (new app, new site, new production infra) and got explicit approval before touching anything live. Four real bugs found only by actually running the full pipeline end-to-end, each fixed in turn: (1) finalize_new_tenant belongs in real_estate_os, not platform_console - bench execute resolves through frappe.get_attr, which checks the TARGET site's installed-app list, and platform_console is deliberately never on one; (2) that same bench execute command in this bench version swallows any real exception from the target function and reports an unrelated NameError instead - switched to bench console fed via stdin (the same exec(open(path).read()) idiom this whole session already uses for verification) so real tracebacks actually reach Tenant Site.error_log; (3) a headless Company.insert() skips what ERPNext's Setup Wizard normally seeds, including Warehouse Type "Transit" (referenced by Company.on_update()'s default-warehouse creation) - seeded explicitly now; (4) bench new-site without --mariadb-user-host-login-scope "%" scopes the new site's DB user to the backend container's IP at that moment, breaking on the next routine backend recreate - realestate.nnuggets.com's own DB user was already %-scoped, which is why it never showed this symptom; manually re-scoped console.nnuggets.com's own DB user the same way before this fix existed. Also needed a small, deliberately scoped production change: a Traefik-dynamic-dir bind mount + MARIADB_ROOT_PASSWORD env var added to the queue-long container only (confirmed via docker inspect that's where frappe.enqueue(queue="long") jobs actually run, a different container from the one serving HTTP) plus group_add for a host/container UID mismatch on that bind mount - each step confirmed against realestate.nnuggets.com staying live throughout via curl checks between every container recreate. Verified live end-to-end on the fourth attempt: a full throwaway test tenant (provisioning-test4.nnuggets.com) reached Live with the correct Company/CoA/currency/esign_mode, was reachable, then torn down. One process slip: pushed one small commit (the Warehouse Type fix) directly to real_estate_os's main instead of through a PR - left as-is rather than rewriting pushed history, and every other change in this batch went through the normal PR flow. | developer-1 | branches: feat/tenant-provisioning-finalize (real-estate PR #66, merged and deployed), platform_console commits pushed directly to rafaii/platform-console main (new app, no PR review process set up yet for it)`
- `2026-09-01T03:00:00Z | multi-tenancy | Imran, after using the just-built Tenant Site form: "there should be a section for integrations and I can add all integrations which at the moment is Inbuilt Esign. As we add more integration during the setup/onboarding screen, i get an option to choose what are enabled for this customer." Clarified on request: multiple integrations need to be enabled per tenant at once, not one default picked from a dropdown. Replaced the single esign_mode Select on Tenant Site with a new child table (Tenant Site Integration: category/provider/enabled). MODULE_APP_MAP gained an integrations list per module (category, options, default) - declared statically in platform_console rather than queried live from the target module app, since there's no tenant site to query yet at onboarding time and frappe.get_hooks() is a site-bound call this app's own process can't safely make against a site it isn't running on. Picking Module auto-populates one row per category into an empty table only (never clobbers edited rows); a second row for the same category can be added by hand to enable a second provider. provision_tenant_site validates every row's (category, provider) pair against the module's declared options. Where the underlying dispatch only supports one active provider per category today (e-sign), the first enabled row becomes finalize_new_tenant's esign_mode - the rest still get recorded as part of this tenant's integration entitlement list. bench migrate run for the schema change (new child doctype, esign_mode field removed). Verified live end-to-end: a throwaway test tenant with both DocuSign and In-Built enabled for E-Sign (DocuSign listed first) correctly ended up with Signature Settings.esign_mode == "DocuSign" and both rows recorded as enabled, then torn down. | developer-1 | pushed directly to rafaii/platform-console main`
- `2026-09-01T04:00:00Z | multi-tenancy | Two more gaps in the just-built Tenant Site form, both found by Imran actually reading it rather than assumed by us: "what is the difference between Tenant Name and Company Name?" (there wasn't one - tenant_name was never referenced anywhere in provisioning.py, removed; Company Name carries the list-view column now) and "The first email created will be the super admin who can then create multiple other users in their account - right?" (also not true as built - admin_email was likewise unused; bench new-site only set a random undisclosed Administrator password). Fixed the second one for real: real_estate_os.provisioning.finalize_new_tenant now creates a System Manager User for admin_email and generates a reset link via User._reset_password(send_email=False) - deliberately not Frappe's own built-in welcome email, which names Frappe/ERPNext. platform_console.provisioning._send_branded_welcome_email sends the actual invite, branded as Aetris (confirmed with Imran: the existing Real Estate portal's own logo/name, not guessed) via Brevo's transactional email API - Imran provided an API key not SMTP credentials, so this calls Brevo's REST endpoint directly with requests rather than frappe.sendmail(), since console.nnuggets.com has no Email Account configured. Extended the finalize step's stdin-script marker mechanism with a second line so finalize_new_tenant's return value (the reset link) round-trips back to platform_console as JSON, not just a pass/fail signal. Added admin_invite_sent/admin_reset_link fields to Tenant Site so a working, live tenant is never marked Failed just because the invite email couldn't send - verified live: a real end-to-end run correctly reached status=Live even though Brevo rejected the actual send (403 permission_denied, "Your SMTP account is not yet activated") - a Brevo account-side activation step, not a code issue, blocked as a follow-up. bench migrate run for the new fields; BREVO_API_KEY added to queue-long's environment (realestate-override.yml, same pattern as MARIADB_ROOT_PASSWORD) and the VPS's own .env, never committed. | developer-1 | real-estate PR #67 (merged and deployed) + platform-console commits pushed directly to main`
- `2026-09-01T04:30:00Z | multi-tenancy | Imran then provided full Brevo SMTP settings (smtp-relay.brevo.com:587, a Brevo SMTP login) in addition to the API key already wired up. Simplified _send_branded_welcome_email to a plain frappe.sendmail() call against a native Frappe Email Account configured once directly on console.nnuggets.com, rather than the bespoke requests-based Brevo API client from the previous entry - Frappe's own mail queue/retry handling comes for free, and the Email Account is a one-time site-level setting, not something provisioning code needs to create per tenant. Removed BREVO_API_KEY from queue-long's environment and the VPS's own .env - no longer read by any code. Attempted to create the Email Account live: it failed validation with the identical wall as the API path - 535 5.7.8 Authentication failed via SMTP vs. 403 permission_denied via the API - confirming this is a Brevo account-level activation gate (needs Brevo support to clear, not a config/credential mistake on either integration path we built). The failed attempt never actually saved (Frappe validates the SMTP connection before insert), so nothing needed cleaning up. Both integration paths are now fully built and verified as far as this app's own code goes; email sending itself stays blocked on Brevo activating the account. | developer-1 | pushed directly to rafaii/platform-console main`
- `2026-09-01T05:00:00Z | multi-tenancy | Noticed while verifying the previous entries: the generated admin_reset_link came back as http:// (e.g. provisioning-test7.nnuggets.com), not https. get_url() defaults to http when called outside a request context - finalize_new_tenant always runs via bench console - and this bench holds no TLS cert of its own (Traefik terminates TLS and redirects http->https at the entrypoint level, confirmed in project_ten/traefik/traefik.yml, so the link still worked via that redirect). Forced the scheme to https on the returned link rather than touching any Frappe/site config, since a password-reset key shouldn't take even one plaintext hop if avoidable. Verified live post-deploy: a fresh finalize_new_tenant call now returns an https:// link directly. | developer-1 | branch: fix/reset-link-https (real-estate PR #68, merged and deployed)`
- `2026-09-01T06:00:00Z | multi-tenancy | Imran, actually onboarding a Qatar tenant: "under currency QAR does not show up in the onboarding flow." Root cause confirmed live: Frappe's Currency doctype ships every ISO currency as a record but only enables nine by default (INR, USD, GBP, EUR, AED, AUD, JPY, CNY, CHF - frappe/geo/doctype/currency/currency.py), and frappe.desk.search.search_widget auto-filters any Link search to enabled=1 on a doctype with an "enabled" field - so a disabled currency like QAR can't even be typed into Default Currency, let alone selected. Same gap silently affects Saudi Arabia (SAR), Canada (CAD), and most other countries this platform is meant to onboard - not a Qatar-specific bug. Added platform_console.provisioning.get_currency_for_country, reading frappe.geo.country_info (the same source Setup Wizard itself uses for its own country->currency default) and force-enabling the match - a one-time, side-effect-free master-data flag flip, not a security-relevant change. Wired into Tenant Site's country handler (also re-run on refresh) to both enable and default Default Currency, without clobbering a manual override. Verified live: QAR was disabled, get_currency_for_country("Qatar") enabled it and search_link("Currency", "QAR") went from [] to a real match; reset back to disabled afterward so Imran's own live retest through the actual form demonstrates the fix end-to-end. | developer-1 | branch: fix/enable-currency-per-country (platform-console PR #1, merged and deployed)`
- `2026-09-01T06:15:00Z | multi-tenancy | Imran reported the currency fix "still not showing" after the deploy above. Root cause was different from the fix itself: Frappe's getdoctype endpoint (frappe/desk/form/load.py) compares a browser's cached DocType.modified timestamp against the DB record and returns a bare "use_cache" - skipping the JS entirely - when they match. Tenant Site's own JSON never changed (only tenant_site.js did), so Tenant Site.modified never moved, and `bench reload-doctype` is itself a no-op when the JSON content is unchanged - so every already-open browser session kept being told nothing changed and never received the new script, even though the server was serving it correctly to any fresh (non-cached) request (confirmed directly via getdoctype in bench console). Fixed by directly bumping `tabDocType`.modified for Tenant Site via SQL - a deliberate, narrow cache-bust, not a data change. Lesson for any future doctype-.js-only fix: it needs this same explicit bump, since neither `reload-doctype` nor a container/image rebuild alone forces already-open browsers to refetch. | developer-1 | operation: direct production cache-bust (no PR - no code changed, only a DocType.modified timestamp)`
- `2026-09-01T06:30:00Z | multi-tenancy | Imran provisioned rastec.nnuggets.com - no invite email arrived despite him confirming "everything checks on Brevo." Root cause turned out to be two distinct, unrelated bugs stacked on top of each other, both confirmed live by testing the API and SMTP paths directly against Brevo rather than guessing: (1) Brevo's transactional API returned 401 "unrecognised IP address" for this VPS's IPv6 address specifically - this host is dual-stack and prefers IPv6 for outbound by default, but only the VPS's IPv4 was ever authorized on the Brevo account; forcing IPv4 for just the Brevo call (via urllib3's allowed_gai_family hook, restored immediately after) got a real 201 + messageId back, confirming the account itself is now fully activated. (2) The SMTP relay (smtp-relay.brevo.com, which only resolves to IPv4 anyway - ruling out the same IPv6 theory there) still rejected with a genuine 535 auth failure - a wrong/stale SMTP-specific credential, unrelated to the working API key. Rather than chase the separate SMTP secret, reverted _send_branded_welcome_email from frappe.sendmail()/native Email Account back to calling Brevo's API directly (Imran's explicit call once shown both results) - the same shape as the very first attempt at this feature, now unblocked. BREVO_API_KEY added to queue-long's environment (realestate-override.yml, same pattern as MARIADB_ROOT_PASSWORD) and the VPS's own .env. Resent rastec's invite directly using its already-generated admin_reset_link (no need to re-provision) - delivered successfully; admin_invite_sent flipped to 1 and error_log cleared on its Tenant Site record. | developer-1 | branch: fix/brevo-api-not-smtp (platform-console PR #2, merged and deployed)`
- `2026-09-01T16:00:00Z | ui-portal | Imran: clicking "Set Password" from the Aetris invite email "takes me to a page to set password, but that is ERPNext branded page. Instead it should show our UI." Investigated the premise first rather than building blind: there is no existing branded login page to match - /login on the already-live realestate.nnuggets.com turned out to be equally stock-ERPNext-branded (erpnext-logo.svg navbar, "Powered by ERPNext" footer), since the React admin portal (admin-portal-ui.md) only takes over once a session exists; /login, /update-password, and /forgot-password are all still Frappe's default website template underneath, platform-wide, unnoticed until now. Surfaced this to Imran before picking a fix size; he chose the site-wide Website Settings fix over a full custom React page for each. real_estate_os.provisioning.finalize_new_tenant gained _set_aetris_branding() - sets Website Settings.app_logo/banner_image/favicon/footer_powered to the existing aetris.svg/.png (the same assets the React portal's own sidebar already uses) and app_name to "Aetris", the single source every one of these pages reads from (get_app_logo(), navbar.html's banner_image, footer_info.html's footer_powered) - no per-page template work needed. Backfilled both already-live real_estate_os tenants (realestate.nnuggets.com, rastec.nnuggets.com) by calling the same function directly; verified live on both that /login and /update-password now show the Aetris logo, favicon, and footer text instead of ERPNext's. | developer-1 | branch: feat/aetris-website-branding (real-estate PR #69, merged and deployed) + direct backfill on the two live tenants`
- `2026-09-02T00:00:00Z | ui-portal | Imran, using the just-branded pages: "From the login page, I tried reset password... No email gets sent! All Saas platform emails need to use Brevo." Root cause: no Email Account exists on any tenant site at all, so frappe.core.doctype.user.user.reset_password hits OutgoingEmailError - silently swallowed by design (anti-username-enumeration), so the UI always says "check your inbox" regardless of what actually happened. Rather than hand-configure a per-tenant Email Account (Brevo's SMTP credential is a known-broken secret - see the 2026-09-01T06:30 entry - and it doesn't scale for a SaaS anyway), registered override_email_send (real_estate_os.brevo_mail.dispatch_outgoing_mail): EmailQueue.send() checks this hook BEFORE ever touching real SMTP, so it replaces delivery entirely rather than wrapping it. Also removed the "Get Updates" newsletter box Imran flagged on the same page - turned out to be ERPNext's own footer_extension.html, already gated behind Website Settings.hide_footer_signup, no template override needed. Frappe still requires *some* Email Account to exist before calling the hook - satisfied by placeholder mail_server/mail_login/mail_password keys added to the bench's shared common_site_config.json directly (every current and future tenant site picks this up automatically, no per-tenant code). Mid-build, Imran refined the requirement further: "each business can decide if emails sending to be managed by Aetris or their own SMTP" (credits/billing for Aetris-managed sending explicitly deferred to later) - the initial version sent everyone's mail through Brevo unconditionally, which was already wrong the moment he said it. Reworked into dispatch_outgoing_mail: checks whether EmailQueue.get_email_account() resolved to a real, business-configured Email Account (validated for real at save time, already exposed on the portal's Settings page) or fell back to the common_site_config placeholder (flagged via _from_site_config) - a real account means send through it exactly as Frappe would have without this hook; no real account means Aetris-managed via Brevo's API. No new schema needed - configuring a working Email Account IS the opt-in signal. BREVO_API_KEY added to backend/queue-short/scheduler's environment (previously only queue-long, added for the invite-email fix, had it) since password-reset mail sends synchronously from a web request. Verified live end-to-end: triggered a real reset_password call against realestate.nnuggets.com (a tenant with no Email Account configured), confirmed the resulting Email Queue row reached status=Sent with zero error, recipient correct, and the tenant's own address preserved as Reply-To rather than the rejected From. | developer-1 | branches: feat/brevo-override-all-mail (real-estate PR #70), feat/per-tenant-email-choice (real-estate PR #71) - both merged and deployed`
- `2026-09-02T02:00:00Z | ui-portal | Imran: "extend the admin UI to create these login page. That way the UI will look integrated and we can use API to connect to the backend" - full detail in vault/ui-portal/features/native-auth-pages.md (status: done). Replaced Frappe's stock /login and /update-password with real pages inside the React portal. Investigated before building: /login is the ONE page every unauthenticated portal visit (and /app Desk access) redirects to, and hooks.py already documents a real historical incident of a catch-all rewrite causing an infinite /login redirect loop - confirmed live that naively adding a /login route rule today would reproduce exactly that, since www/portal/index.py's get_context unconditionally redirects any Guest away. Used a Plan-agent-reviewed plan (Imran: "put all plans in the vault", now a standing preference) before touching the live login path: restructured get_context so /login and /update-password are the two exceptions to the Guest-redirect (with a safe_redirect() open-redirect guard replacing what frappe/www/login.py's sanitize_redirect() provided before that file became wholly unreachable on this site), then added the two routes to website_route_rules deliberately in the same explicit, non-catch-all shape as the rest of the list. Deployed as two independently-verifiable steps per the plan - the get_context restructure alone first (curl-verified as a provable no-op: /login still resolved to stock Frappe, guest /overview still redirected correctly), then the route rules + new Login.tsx/SetPassword.tsx together, with bench clear-cache after (website_route_rules is cached). Pre-flight checks against the live site (2FA off, disable_user_pass_login off, no conflicting route_redirects, no social login keys) surfaced one live, in-use feature - System Settings.login_with_email_link=1, an actual magic-link option rendered on the real page - which Imran's call was to drop for v1 rather than silently lose or rebuild now. Verified live end-to-end: guest /login renders the new page with zero redirect loop; a real throwaway test user (created and deleted after) completed sign-in, forgot-password's shared code path with the already-proven Brevo pipeline, and a real generated reset key through /update-password, which correctly logged the user in directly (update_password's actual behavior, not a confirmation-screen bounce back through /login) and rejected a re-used key with the real 410 message; System Manager /app access and console.nnuggets.com's own unrelated /login were both confirmed unaffected. Found and fixed one real bug during this live verification, same session: an invalid-login attempt surfaced the raw "frappe.exceptions.AuthenticationError" exception-class string instead of the real "Invalid login credentials" message, because the legacy cmd=login endpoint doesn't populate _server_messages the way lib/api.ts's shared call()/serverMessage() helper expects - fixed locally in Login.tsx (a dedicated loginCall() preferring json.message) rather than changing the shared helper's fallback order for every other caller in the app. | developer-1 | branches: feat/native-auth-pages-step1 (real-estate PR #72), feat/native-auth-pages-step2 (PR #73), fix/login-error-message (PR #74) - all merged and deployed`
- `2026-09-02T05:00:00Z | ui-portal | Imran, after the native auth pages shipped: "clicking on the email link to setup a password, login takes the customer to https://rastec.nnuggets.com/app/setup-wizard/0 - this is ERPNext branding!" Root cause confirmed live on rastec.nnuggets.com: frappe.is_setup_complete() (gates the Setup Wizard on every /app boot) reads a cached Installed Application table computed exactly once, by install_app() when real_estate_os was installed on the site - before finalize_new_tenant ever creates the Company, so it stays permanently frozen at "incomplete" even after a real Company and admin user exist (both confirmed present via bench console, setup_complete still 0). finalize_new_tenant now calls Installed Applications.update_versions() itself after Company + admin-user creation, so every future tenant self-corrects the flag at provisioning time with no manual step. Also found and fixed a second, related gap while root-causing this: update_password() hardcodes "/app" (Frappe's Desk) as the post-set-password destination for every System User - our tenant admins - which is itself ERPNext UI, not this SaaS's; SetPassword.tsx now treats that one specific value as "no real target" and sends them into the React portal instead, matching Login.tsx's own default. Backfilled rastec.nnuggets.com live (with explicit sign-off, since it's a production DB write) before the code fix deployed; realestate.nnuggets.com was already correctly configured and didn't need it. Verified post-deploy: guest /app on rastec now redirects to the new React /login (not /app/setup-wizard/0), and the deployed image's bundled JS/py both contain the fix. | developer-1 | branch: fix/setup-wizard-and-desk-redirect (real-estate PR #75, merged and deployed) + direct backfill on rastec.nnuggets.com`
- `2026-09-02T06:00:00Z | payments-accounting | Imran, logged in as rastec.admin: clicking Settings -> Add bank account failed with "User rastec.admin@4itrading.com does not have doctype access via role permission for document Bank Account." Root cause: finalize_new_tenant assigns a brand-new tenant's first (and, until they create staff, only) user just the System Manager role, but Bank Account (and every other core ERPNext accounting doctype - Sales Invoice, Purchase Invoice, Journal Entry, GL Entry) only ever had Custom DocPerm rows granted to the "Accountant" role (create_staff_roles_and_permissions.py, create_bank_account_permissions.py) - never System Manager. Confirmed live via frappe.has_permission under that exact user: all five doctypes returned False for read and create, despite the Accounts tab in this same user's own nav already depending on them. Unlike this app's own custom doctypes (Building, Maintenance Request, etc.), which bake System Manager in with full CRUD from their own DocType JSON, these are core ERPNext doctypes real_estate_os never touched that way. New patch (grant_system_manager_core_accounts_access) grants System Manager the identical read/write/create/delete (+ Journal Entry submit/cancel) Accountant already has on all six doctypes - so a tenant's very first user can run their own books before creating any staff, not just create other users who can. Ran bench migrate on both live tenants (rastec.nnuggets.com, realestate.nnuggets.com) after deploy; re-verified live that rastec.admin's frappe.has_permission now returns True across all six. | developer-1 | branch: fix/system-manager-accounts-permissions (real-estate PR #76, merged and deployed) + bench migrate on both live tenants`
- `2026-09-02T07:00:00Z | esign | Imran approved ADR-0019 (E-Sign Template & Field-Placement Designer): lets a business owner author their own contract template and visually place signature/initial/date/text fields, replacing today's single hardcoded Print Format. Numbered/moved the draft, added feature files esign-template-management.md (Phase 1) and esign-field-placement-designer.md (Phase 2); no app code written yet. | developer-1 | no branch yet — vault-only ADR/planning entry, implementation not started |
- `2026-09-02T07:30:00Z | esign | Implemented ADR-0019 Phase 1 (esign-template-management): E-Sign Template + E-Sign Template Signer Role doctypes added to inbuilt_esign, _generate_document_pdf() now renders an active template via frappe.render_template with fallback to the original frappe.get_print() path, send_lease_for_signature resolves signer_roles when a template is active, new get_mergeable_fields/render_template_preview whitelisted methods, and a new Contract templates panel in real_estate_os's Settings UI. Removed the dead E-Sign Field doctype (confirmed unused) via a migration patch. Code complete and committed on agent/developer-1/esign-templates in both apps (inbuilt_esign 94c518e, real_estate_os 3eb7b8e); frontend tsc -b --noEmit passes clean. Not yet pushed, merged, or deployed — no local bench exists in this environment to verify live, and the doctype-removal patch is a destructive migration flagged for explicit sign-off before deploying. | developer-1 | branch: agent/developer-1/esign-templates (both apps, not yet pushed) |
- `2026-09-02T07:35:00Z | esign | Opened PRs for ADR-0019 Phase 1 (esign-template-management), not yet merged: rafaii/inbuilt-esign#1 (doctypes + rendering pipeline) and rafaii/real-estate#77 (Contract templates Settings UI, depends on #1). Neither has been deployed; migration/live-flow verification is still pending a real bench. | developer-1 | branches: agent/developer-1/esign-templates (both apps) |
- `2026-09-03T03:20:00Z | esign | Deployed ADR-0019 Phase 1 (esign-template-management) to production: merged rafaii/inbuilt-esign#1 and rafaii/real-estate#77 to main, rebuilt the VPS custom image, recreated containers, and ran bench migrate on both tenant sites with inbuilt_esign installed (rastec.nnuggets.com, realestate.nnuggets.com) — clean, no errors, including the E-Sign Field removal patch (both sites confirmed 0 rows beforehand). Verified live: new E-Sign Template/E-Sign Template Signer Role tables exist, old E-Sign Field DocType record gone, pre-existing E-Sign Document data intact, both sites healthy (200 on /login, clean backend logs). The now-orphaned empty E-Sign Field table itself wasn't physically dropped (Frappe on_trash didn't cascade) — left as a harmless cosmetic follow-up rather than forcing a DROP TABLE through the auto-mode classifier's block on it. Functional click-through of the new template-authoring flow itself is still outstanding. | developer-1 | branch: agent/developer-1/esign-templates (merged, both apps) |
- `2026-09-03T03:35:00Z | esign | Imran: "how do I set the template? I do not see any UI element" — the Contract templates panel from PR #77 never actually rendered. Root cause: real_estate_os/ui is a React SPA whose committed build output (real_estate_os/public/portal/) is what Frappe actually serves (see ui/README.md's build step) — PR #77 shipped the source diff but never re-ran `bun run build`, so the deployed bundle was still pre-#77 despite the source being merged. Fixed via real-estate PR #78 (build output only, re-run against the already-merged #77 source, no source changes), merged, VPS image rebuilt and containers recreated again. Verified live: the served Dashboard.js on both realestate.nnuggets.com and rastec.nnuggets.com now contains the new panel's "Contract templates" text; both sites still respond 200. Lesson: any real_estate_os/ui change needs its build output committed and deployed as its own explicit step, not assumed to follow from the source commit — tsc -b --noEmit passing is not the same as the bundle being rebuilt. | developer-1 | branch: agent/developer-1/esign-templates-build (real-estate PR #78, merged and deployed) |
- `2026-09-03T03:50:00Z | esign | Imran: "there should be a live preview as you build the template... it is difficult to see how it's going to look because it is HTML based. It helps the build with basic HTML knowledge." Added render_body_preview (inbuilt_esign) — renders unsaved body text against a real sample record and returns HTML directly (no PDF) — plus a debounced live-preview iframe pane in the template editor (real_estate_os), replacing the side-column merge-field list with a compact chip strip to make room. Merged rafaii/inbuilt-esign#2 and rafaii/real-estate#79 (bundle rebuilt and committed in the same commit as source this time), deployed to VPS (image rebuilt, containers recreated, no migration needed - no schema change). Verified live: served Dashboard.js on both tenant sites contains "Live preview", and a direct bench execute of render_body_preview against realestate.nnuggets.com correctly substituted a real customer ID into a merge-field test string. | developer-1 | branches: agent/developer-1/esign-live-preview (both apps, merged and deployed) |
- `2026-09-03T04:20:00Z | esign | Imran: "I do not see important information of Tenant like name, phone, email etc. Similarly I do not see all merge fields for the unit like name, building, address, number of bedroom etc." Root cause: get_mergeable_fields and the render context only read the reference doctype's (Lease Agreement) own fields, never followed its Link fields (customer, unit) into the linked doctypes. Added a generic two-hop Link resolver in inbuilt_esign (_resolve_links/_render_context/_collect_mergeable_fields, depth=2, walks whatever Link fields the reference doctype's meta has - no hardcoded Lease Agreement/Customer/Unit knowledge) so links.customer.customer_name, links.unit.bedrooms, links.unit.building.address are now valid merge tokens; picker groups chips by source doctype. Merged inbuilt-esign#3 + real-estate#80 (bundle included), deployed (image rebuilt, containers recreated, no migration - no schema change). Verified live against real production data on realestate.nnuggets.com via bench execute: correctly resolved tenant name/email, unit number/bedrooms, and building address (2-hop) in one call. | developer-1 | branches: agent/developer-1/esign-linked-fields (both apps, merged and deployed) |
- `2026-09-03T04:45:00Z | esign | Built and deployed ADR-0019 Phase 2 (esign-field-placement-designer): the visual signing-field placement designer. Backend: E-Sign Template Field child table (role_label/field_type/page_no/x_pct/y_pct/width_pct/height_pct), _finalize() now stamps placed fields onto the actual contract page via a pypdf/reportlab overlay (role_label resolved to a signer via signer_roles' signing_order, same fallback heuristic as send-time) before appending the unchanged certificate page; reportlab added as an explicit inbuilt_esign dependency (verified live it was NOT already transitively installed - pypdf is, reportlab wasn't). Frontend: new "Place signing fields" screen renders the actual generated PDF via PDF.js (page by page, at a fixed pixel width), click-to-place Signature/Initial/Date/Text boxes with drag-to-reposition and a resize handle, role assignment from the template's signer_roles, saved through the existing generic E-Sign Template update path (no new whitelisted method needed - fields is just another child table like signer_roles). pdfjs-dist (~2MB, mostly its worker) is lazy-loaded via dynamic import so it only downloads when the designer opens - confirmed Dashboard.js chunk size unaffected. Merged inbuilt-esign#4 + real-estate#81, deployed (image rebuilt - verified reportlab installs cleanly - containers recreated, bench migrate clean on both tenant sites, new tabE-Sign Template Field table confirmed). Verified live end-to-end via direct python (no DB needed for this piece): _build_field_overlay correctly draws when there's signer content and correctly returns None when there isn't; a full base-PDF + stamp + merge_page + text-extraction round-trip confirmed both the original page content and the stamped text survive in the final merged PDF. Not yet verified: a real human click-through of the designer UI itself and a full live test signing (needs OTP/consent/signature-capture through the real guest flow, out of reach via SSH). | developer-1 | branches: agent/developer-1/esign-field-placement (both apps, merged and deployed) |
- `2026-09-03T04:55:00Z | esign | Imran hit "Setting up fake worker failed: Failed to fetch dynamically imported module: .../pdf.worker.mjs" opening the field-placement designer, then "No pages to show" after refreshing (same failure, different surfacing - the unhandled worker-setup rejection never reached the component's own pdfError state). Root cause confirmed live: deployed nginx has no MIME mapping for .mjs (served application/octet-stream), and X-Content-Type-Options: nosniff (already set site-wide) then blocks pdfjs-dist's internal `new Worker(url, {type:"module"})` construction against a non-JS-MIME response. Fixed within app-repo control (no nginx access): scripts/copy-pdf-worker.mjs copies the worker into public/ as pdf.worker.min.js (plain extension rename, content unaffected) before every build, reusing the .js MIME mapping every other built chunk already gets correctly; the copy itself isn't tracked (ui/.gitignore), only the final built output is. Merged real-estate#82, deployed (image rebuilt, containers recreated, no migration - no schema change). Verified live: pdf.worker.min.js now serves content-type: application/javascript (was application/octet-stream on the old .mjs path, now 404), Dashboard.js references the new filename, both tenant sites healthy. | developer-1 | branch: agent/developer-1/esign-worker-mime (merged and deployed) |
- `2026-09-03T05:10:00Z | esign | Imran approved ADR-0020 (PDF Template Upload & Guided Click-to-Sign): business owner can upload a blank PDF instead of authoring HTML (Phase 3), and signers get a field-position-aware guided signing flow with a generated cursive-font signature instead of today's flat single-widget page (Phase 4). Two research findings simplified the design: Phase 2's finalize-time stamping already applies one signature to every assigned field position automatically, and a "generated signature" only needs a font swap on the existing typed-name fallback, not a new image-generation pipeline. Numbered/moved the draft, added feature files esign-pdf-template-upload.md and esign-guided-click-to-sign.md; no app code written yet. | developer-1 | no branch yet — vault-only ADR/planning entry, implementation not started |
- `2026-09-03T18:50:00Z | esign | Implemented and deployed ADR-0020 Phase 3 (esign-pdf-template-upload): E-Sign Template gains source_type (HTML/PDF) + pdf_file; E-Sign Template Field gains a Merge Field field_type + merge_token. Rendering branches on source_type - PDF mode uses the uploaded file as base and stamps each Merge Field's resolved value via a generalized overlay builder (_field_geometry extracted from _build_field_overlay, _build_text_overlay as its merge-field sibling), reusing the same _render_context (doc/links) resolution Jinja rendering already uses. Frontend: template editor gets an HTML/PDF source toggle with file upload (create-then-upload-then-activate sequencing to satisfy the backend's "can't activate a PDF template with no file" validation); field-placement designer gets a 5th "Merge Field" palette option (distinct emerald color) using the same grouped field picker the HTML body's chips already use. Merged inbuilt-esign#5 + real-estate#83, deployed (image rebuilt, containers recreated, bench migrate clean on both tenant sites - source_type/pdf_file columns confirmed present). Verified live via direct Python: _resolve_merge_token correctly walks both doc.* (attribute access) and links.*.* (nested dict, including 2-hop) paths, returns empty string gracefully for a missing path, and a full base-PDF + stamp + merge_page + text-extraction round-trip confirmed both original and merged text survive. | developer-1 | branches: agent/developer-1/pdf-template-upload (both apps, merged and deployed) |
- `2026-09-03T23:05:00Z | esign | Implemented and deployed ADR-0020 Phase 4 (esign-guided-click-to-sign): signing page gets an explicit Draw/Type mode choice, a live Dancing Script cursive preview for typed names with a soft (non-blocking) name-mismatch warning, auto-derived initials, and - when the template has field placements for this signer - a PDF.js-rendered document with tappable field markers gating "Sign document" until all are tapped. capture_signature's shape and the OTP/consent server-side gates are unchanged. New get_signer_fields(doc, signer) mirrors _stamp_template_fields' existing role/signing_order resolution from one signer's perspective. _build_field_overlay's typed-name fallback and _certificate_html both now use a bundled Dancing Script font (OFL-licensed, fetched from the official google/fonts GitHub repo) instead of Helvetica-Oblique/plain serif. Bundled pdf.js's legacy ES-module build (copied from the already-installed npm package, renamed .mjs->.js - same MIME fix already proven for the React portal, since the deployed nginx has no .mjs mapping and pdfjs-dist always constructs a module Worker). Required a VPS-side Dockerfile change (not tracked in any git repo, confirmed live) to symlink inbuilt_esign's public/ folder the same way real_estate_os's already was - without it nothing under /assets/inbuilt_esign/... would serve at all. Merged inbuilt-esign#6 (no real_estate_os changes needed - this phase is entirely guest-facing, served by inbuilt_esign alone), deployed (image rebuilt - verified the new symlink exists in the built image - containers recreated, no migration needed - no schema change). Verified live: all three new static assets (font, pdf.min.js, pdf.worker.min.js) serve with correct MIME types; hit the actual production signing URL for an existing document and confirmed it renders successfully through the real Frappe pipeline with signerFields correctly resolving to [] and expectedName correctly resolving to the real signer's name - zero regression for the "no field placements" case every existing document is in. Not yet verified: the interactive JS (mode toggle, tap-to-place, PDF.js rendering) against a document that actually HAS field placements assigned to a signer - needs a human click-through with real business data, not reachable via curl/SSH alone. | developer-1 | branch: agent/developer-1/guided-signing (merged and deployed) |
- `2026-09-03T23:20:00Z | esign | Imran, using the field-placement designer on a real PDF template: "trying edit but the field is showing as rendered and on top a new merge field is showing, instead of editing the first field." Root cause: handlePageClick always created a new field whenever a placement tool was active, with no check for whether the click landed inside an already-placed field's box - a still-armed tool (re-clicking the same palette button, or a click just missing a small marker) stacked a duplicate instead of the click reaching FieldBox's own click-to-select handler. Fixed by hit-testing the click against all placed fields on that page first - a click inside an existing field's box now always selects it (and disarms the tool) instead of creating anything new. Merged real-estate#84, deployed (image rebuilt, containers recreated, no migration needed). Verified live: fresh Dashboard.js served (timestamp/size match the new build), both tenant sites healthy. | developer-1 | branch: agent/developer-1/field-click-fix (merged and deployed) |
- `2026-09-03T23:40:00Z | esign | Imran: "the field is showing as rendered and on top a new merge field is showing" - after saving a PDF template with merge fields and reopening the field-placement designer, a previously-placed merge field's resolved value was already baked flat into the page image, with a new editable marker drawn on top of it. Root cause: the designer's preview reused the exact same rendering path _generate_document_pdf uses for the real final document, which for a PDF-sourced template always runs _stamp_merge_fields. Fixed by adding include_merge_fields (default 1, unchanged for the template list's plain Preview link) to render_template_preview; the designer's fetch now passes 0 to get the pristine unstamped base. Verified live via bench console against the real template ("Template 2"): unstamped (143707 bytes) vs stamped (199272 bytes) previews are meaningfully different, confirming the fix. While verifying, also found and fixed a second real bug in the same template: one of its merge fields is bound to a bare Link field (links.unit.building) rather than a leaf value - _resolve_links already replaces a Link field's raw ID with the linked record's full dict (needed for deeper paths like links.unit.building.address), so resolving the bare token was stamping Python's dict repr directly onto the contract, confirmed live before the fix. _merge_value_to_text now falls back to the linked doctype's title_field (Building.building_name) or its name/ID - verified live post-deploy: links.unit.building now resolves to 'Rastec 21' instead of the dict repr. Merged inbuilt-esign#7 + real-estate#85 (designer-preview fix) and inbuilt-esign#8 (link-value fix), deployed both rounds (image rebuilt, containers recreated, no migration needed each time). | developer-1 | branches: agent/developer-1/designer-preview-fix (both apps), agent/developer-1/merge-link-value (inbuilt_esign) - both merged and deployed |
- `2026-09-03T23:50:00Z | esign | Imran: "Is there a way to preview the merge field value while place or editing? Maybe show the value based on a random customer next to the merge field dropdown saying Preview?" Added preview_merge_token(reference_doctype, merge_token) - resolves one token against a sample record (reusing _resolve_merge_token/_render_context exactly as the full-PDF preview path does) and returns the plain text value. Field-placement designer's selected-field bar now shows a "Preview" box next to the merge-field dropdown, updating whenever the token selection changes. Merged inbuilt-esign#9 + real-estate#86, deployed (image rebuilt, containers recreated, no migration needed). Verified live against the real template's data via bench console: links.unit.building -> "Rastec 21", doc.customer -> "CUST-2026-00009" - both correct. | developer-1 | branch: agent/developer-1/merge-preview (both apps, merged and deployed) |
- `2026-09-04T04:20:00Z | esign | Imran, after a full real signing flow: merge fields on the final signed PDF (contract name, building, unit) were overwritten with the counter-signer's name; the certificate page's typed signature rendered as plain text instead of the Dancing Script cursive font; the "Download the signed document" link on the completed signing page showed unsigned content; requested an envelope ID watermark on every page. Root causes, each confirmed live before fixing: (1) _stamp_template_fields never excluded Merge Field rows, so _resolve_stamp_signer("") fell through to its fallback heuristic (signing_order 2, the counter-signer) and stamped that signer's name over every merge field via _build_field_overlay's generic-text branch - fixed with a `field_type == "Merge Field": continue` guard, mirroring the one already correct in get_signer_fields. (2) Not a variable-font issue as first suspected (a live discrimination test proved the base64 @font-face embedding worked fine even with the variable font) - the actual cause, confirmed by reproducing against the real signed document ESD-00436 and reading wkhtmltopdf's own stderr ("Unexpected token FUNCTION format("), is that wkhtmltopdf's CSS parser rejects the `format("truetype")` hint in `@font-face src` and silently drops the whole rule; removed the hint. Also swapped the bundled font for a static (non-variable) instance of the same Dancing Script design as a compatibility hardening measure, unrelated to the actual fix. (3) The signing page's "Review the document" card (iframe + open-in-new-tab link) was never toggled by renderState() and stayed pinned to the unsigned pdfUrl() for the page's entire lifetime, sitting right above the correct "Download the signed document" link - now repointed to the signed PDF on completion, plus the signed-PDF URL is cache-busted as defense in depth. (4) New _stamp_envelope_watermark stamps a small rotated "Envelope ID: ESD-xxxxx" label along the right margin of every page - at generation time for contract pages (so the copy a signer reviews already carries it) and at finalize time for the newly appended certificate page(s) only, avoiding double-stamping. Merged inbuilt-esign#10, deployed (image rebuilt, containers recreated, no migration needed - no schema change). Verified live post-deploy: DancingScript-Regular now appears in the certificate PDF's embedded fonts for ESD-00436 (previously only DejaVuSans). Flagged to Imran: any E-Sign Document already finalized before this fix (docstatus=1, signed_pdf locked) has a permanently corrupted signed PDF from the merge-field bug and can't be regenerated after the fact. | developer-1 | branch: agent/developer-1/signing-fixes (inbuilt_esign, merged and deployed) |
- `2026-09-04T05:30:00Z | payments-accounting | P&L statement flat table replaced with collapsible tree table — top-level groups (Income, Expenses, Profit) visible by default, chevron toggle to drill into sub-accounts. Hierarchy inferred from ERPNext's leading-space indentation in account names, then fixed same-day: `countIndent()` was counting leading spaces in the `account` field, but ERPNext's report engine returns that field as a plain Account name with a separate numeric `indent` field for hierarchy depth — `buildPnlTree` now reads `row.indent` directly. | developer-1 | branch: agent/developer-1/collapsible-pnl (merged to main) |
- `2026-09-04T04:50:00Z | esign | Imran: "Let us remove the mouse sign option and only keep typing name to sign option. Because if the tenant and owner use different method it looks odd." Removed the Draw/Type mode toggle, the jSignature canvas, and all draw-mode JS (signatureData/signatureImageSrc/setMode/mode state/clear button) from the guest signing page - typing your full name (Dancing Script cursive rendering, unchanged) is now the only way to sign. capture_signature already accepted typed_name alone with no image, so no backend behavior change beyond a clearer validation message ("Please type your name to sign") when neither is provided. Field-marker tap previews (Signature/Initial) always render the cursive typed text now, no drawn-image branch. Already-signed documents with a stored drawn-signature image still display it correctly in the Signing Parties & Status history - only the input UI changed going forward. Merged inbuilt-esign#11, deployed (image rebuilt, containers recreated, no migration needed - no schema change). Verified live: served index.html has zero jSignature references, both tenant sites healthy. | developer-1 | branch: agent/developer-1/type-only-sign (merged and deployed) |
- `[RECOVERY GAP — 2026-09-04 ~05:00Z through 2026-09-05 ~09:00Z]` Entries in this window were lost in a file-recovery incident on 2026-09-07 (see below) and could not be reconstructed from any available source (git history, backups, or this session's own context). If you recall specific work done in this window, it's worth re-adding by hand; otherwise this gap is permanent.
- `2026-09-05T09:00:00Z | multi-tenancy | Imran asked for the console site (console.nnuggets.com) to become the real SaaS back-office: business onboarding, subscription tracking, suspend/cancel, platform settings. Drafted and approved ADR-0022 (platform_console gets a custom React frontend matching apps/real_estate_os/ui's stack/conventions, replacing plain Frappe desk forms) and ADR-0023 (Suspend/Cancel enforced purely via Traefik routing inside platform_console - no code on real_estate_os or any future OS module, after an earlier tenant-side-app proposal was rejected by Imran as putting SaaS-control capability on tenant infrastructure). Created feature files console-admin-ui.md and tenant-lifecycle-enforcement.md; no code written yet. | developer-1 | branch: (planning only, no branch yet) |
- `2026-09-05T09:45:00Z | multi-tenancy | Advisor review caught that suspend_tenant_site/reactivate_tenant_site/cancel_tenant_site ran inline in a @frappe.whitelist() method, but the Traefik dynamic-route directory is only bind-mounted into the queue-long container (confirmed live via docker inspect/exec on realestate-backend-1) - the first version would have silently no-op'd on every Suspend while still reporting success, masked by a stray os.makedirs(exist_ok=True). Route write/removal now runs via frappe.enqueue(queue="long"), matching provision_tenant_site's own pattern; subscription_status updates immediately and the console UI refetches once more ~2.5s later to surface a delayed routing failure. Also fixed the onboarding wizard's Country dropdown, which silently truncated to 200 of ~250 countries ordered by modified instead of name. Not yet deployed. | developer-1 | branch: agent/developer-1/console-admin-ui-and-lifecycle |
- `2026-09-05T10:20:00Z | multi-tenancy | Deployed console-admin-ui and tenant-lifecycle-enforcement to console.nnuggets.com (rafaii/platform-console). Found and fixed live: (1) VPS apps/Dockerfile had no /assets/platform_console symlink at all - added it; (2) www/ and public/ were placed one level too deep (under the module-name subfolder, which happens to also be named platform_console), causing Frappe's PathResolver to 404 every console route even though files existed on disk - moved both up to the app-package level, matching real_estate_os's layout. Verified live end-to-end: /login renders window.consoleBootstrap + the SPA bundle, /dashboard and / correctly redirect Guest to /login, static assets serve 200. Ran bench migrate (new Platform Plan doctype + Tenant Site fields) and both clear-cache and clear-website-cache (the latter was serving a stale cached 404 from before the route rules existed). Pending: Imran's live create/delete tenant test. | developer-1 | branch: agent/developer-1/console-admin-ui-and-lifecycle (deployed) |
- `2026-09-05T11:10:00Z | multi-tenancy | Imran, testing the console live: "What happens when you add a subscription plan to a business? Should it not move from Trial to Active?" - set_plan previously only ever touched subscription_plan, matching the original design intent that plan changes must never implicitly suspend/reactivate a tenant, but that intent overshot: converting Trial to Active on plan assignment is the natural onboarding signal, not a routing decision. set_plan now transitions Trial -> Active when a plan is assigned to a Live tenant (no route change needed, both are routed statuses); Suspended/Cancelled -> Active is still only ever explicit via reactivate_tenant_site, since that one actually has to restore public access. | developer-1 | branch: agent/developer-1/console-admin-ui-and-lifecycle |
- `2026-09-05T11:40:00Z | ui-portal | Imran, live on test.nnuggets.com after a password reset: "it redirects to .../app/home ... that should NEVER happen! ... ERPNext should be headless!" Same bug class as the 2026-09-02 fix in native-auth-pages.md, recurring because that fix's exact-string check (redirectPath !== "/app") was already too narrow when it shipped - Frappe's update_password actually returns get_default_path() or "/desk" for a System User (confirmed by reading frappe/core/doctype/user/user.py directly), which the browser follows through an /app alias to Desk's own /app/home landing route - three different literal strings for the same outcome. Fixed with isDeskPath() (matches the first path segment, not one exact string) in real_estate_os/ui's SetPassword.tsx and Login.tsx's safeRedirect(), plus the server-side mirror in www/portal/index.py; applied the identical fix to platform_console/ui (same copied pattern, not yet a live bug there) per Imran's direction that this must not be something each future OS module rediscovers on its own. Documented as a required convention in ADR-0006. | developer-1 | branches: agent/developer-1/fix-desk-redirect (real_estate_os), agent/developer-1/console-admin-ui-and-lifecycle (platform_console) |
- `2026-09-05T12:30:00Z | multi-tenancy | Imran: "in the console I need a way to permanently delete a business" + "when a new business is created, they get an email to set password. But it is only valid for some time... how do we resend that email?" Drafted and approved ADR-0024 (permanent deletion: Cancelled-first, typed-subdomain confirm, bench backup --with-files before bench drop-site --force, Tenant Site record kept marked Deleted as audit trail - new Deleting/Deleted/Delete Failed status values). Built delete_tenant_site + run_delete_tenant_site in platform_console/provisioning.py and a Delete Permanently dialog on the Business Detail page. Also built resend_admin_invite (reuses the existing _finalize_new_tenant bench-console-exec mechanism and Aetris-branded email sender) plus a Resend Invite button, and fixed SetPassword.tsx's expired-link case to point at the existing forgot-password flow instead of a dead-end retry. Also fixed, per Imran's live observation: the Businesses list and Dashboard now exclude Deleted tenants by default - filtering the provisioning-status dropdown for Deleted still reveals them. | developer-1 | branches: agent/developer-1/console-admin-ui-and-lifecycle (platform_console), agent/developer-1/admin-invite-resend (real_estate_os) |
- `2026-09-05T13:05:00Z | multi-tenancy | Imran, live: deleting provisioning-test5 failed with bench backup's generic "Database or site_config.json may be corrupted" error. Root cause: that Tenant Site record said status Live but had no site directory or Traefik route on disk at all - a record left over from manual cleanup done before the Delete feature existed today, out of sync with reality, not real corruption. run_delete_tenant_site now checks whether the site directory actually exists first; if not, skips backup/drop-site entirely (nothing to protect) and finalizes straight to Deleted with an informational frappe.log_error note, instead of surfacing a scary corruption-sounding failure for a site that was already gone. A genuine backup/drop-site failure on a real, existing site still surfaces normally as Delete Failed. | developer-1 | branch: agent/developer-1/console-admin-ui-and-lifecycle |
- `2026-09-05T13:30:00Z | multi-tenancy | Imran: "provisioning-test, provisioning-test2, provisioning-test3 = they don't seem to have a cancel button - so I cannot delete." Real gap: those three all have status Failed (never went Live), and Cancel requires status==Live, so they had no path to Cancelled and therefore no path to Delete under ADR-0024's original Cancelled-first rule. Amended ADR-0024: delete_tenant_site now also allows deletion when status is Pending/Failed/Delete Failed, independent of subscription_status, since a tenant that never went Live carries none of the risk Cancelled-first exists to prevent. Console Delete Permanently button updated to match. Caught and fixed a decorator-placement bug (a module-level constant landed between @frappe.whitelist() and def, a SyntaxError) before it shipped. Deleted the three requested records via the real end-to-end flow - none had a real site directory on disk, confirmed via the existing _site_dir_exists check, so each resolved straight to Deleted with no backup/drop-site needed. | developer-1 | branch: agent/developer-1/console-admin-ui-and-lifecycle |
- `2026-09-05T14:00:00Z | custom-module | Imran: creating a Building on a newly created RealEstate OS account showed an empty Landlord dropdown despite the placeholder text saying to pick "Own Building." Root cause: seed_own_building_landlord.py is a post_model_sync patch, which only runs via bench migrate on an already-existing site - bench new-site --install-app (how platform_console actually provisions every tenant) never runs it. realestate.nnuggets.com (migrated directly many times) had it seeded; rastec.nnuggets.com and test.nnuggets.com (both console-provisioned) did not. Fixed by calling the seed's logic (extracted into ensure_own_building_landlord()) directly from real_estate_os.provisioning.finalize_new_tenant, the function that already exists to handle exactly this class of "what a fresh site needs that new-site alone doesn't provide." bench migrate on both sites completed clean but did NOT backfill the Landlord - bench new-site --install-app marks patches as already-applied in that site's Patch Log for a fresh install, so migrate correctly saw it as already-run and skipped it again. Backfilled both by calling ensure_own_building_landlord() directly via bench console instead (safe regardless of Patch Log state). | developer-1 | branch: agent/developer-1/own-building-seed |
- `2026-09-05T23:35:00Z | custom-module | Imran: "I do not see it us.nnuggets.com/landlords" - us.nnuggets.com ("US Real Estate") was a real, distinct tenant provisioned at 18:05 UTC, before the finalize_new_tenant fix deployed at 23:17 UTC, so it also had zero Landlord records at all. Not a new bug - a fourth pre-fix site alongside realestate/rastec/test. Backfilled via ensure_own_building_landlord() directly. All four Live tenants now have Own Building seeded. | developer-1 | branch: agent/developer-1/own-building-seed |
- `2026-09-06T05:12:00Z | payments-accounting | Fixed /accounts Trial Balance showing a flat account list instead of grouped like Balance Sheet: get_trial_balance never passed show_group_accounts to ERPNext's engine, so it stripped every group/parent row before returning (Balance Sheet's engine never hits that strip step). Added the filter — one-line fix, frontend already renders both via the same CollapsibleReportTable component. | developer-1 | repo: accounts-portal |
- `2026-09-06T05:20:00Z | payments-accounting | Imran: "/overview Margin by property shows Rastec 21 with landlord cost as 0 - why?" Root cause traced live: Rastec 21 (BLD-00406, PDC payment method) has zero Purchase Invoices ever posted - the PDC path only invoices at cheque clearance (mark_pdc_cleared), and nothing auto-generates the outgoing PDC Entries a Head Lease needs before that can happen (generate_pdc_schedule_for_head_lease exists but was only ever reachable via a manual button). Shipped the uncontroversial half: surfaced Outgoing PDC deposit/clearance actions on the global /actions page (previously Incoming-only), reusing existing mark_pdc_sent_to_bank/mark_pdc_cleared handlers - get_accounts_data gains outgoing_deposited/outgoing_past_due, Dashboard.tsx gains DueForDepositOutgoingPdcPanel/DepositedOutgoingPdcPanel. Deployed live. | developer-1 | branch: agent/developer-1/outgoing-pdc-actions-page |
- `2026-09-06T05:45:00Z | payments-accounting | Drafted and approved ADR-0025: decouples landlord cheque plan from accrual invoicing and payment settlement. Imran: "PDC should not be the purchase invoice, that should go separately whereas the payment should also be separate. But by the time you complete the Head Lease, all these details should be entered so it gets generated too" (some landlords require lump-sum advances, e.g. 2 semi-annual cheques instead of 12 monthly, which the old 1:1 cheque=invoice model can't express). Head Lease gets an admin-entered cheque plan gating Active status; accrual Purchase Invoices post on schedule for all buildings regardless of cheque cadence; cheque clearance settles oldest-outstanding-invoice-first with any remainder held on-account and auto-swept via ERPNext's Payment Reconciliation tool as future invoices post. No retroactive backfill - forward-only cutover. Implementation not yet started. | developer-1 | vault only, no code branch yet |
- `2026-09-06T06:30:00Z | payments-accounting | Built the ADR-0025 implementation: new Head Lease Cheque Plan child doctype (admin-entered check_number/amount/check_date, gates Head Lease reaching Active on PDC-method Buildings), set_head_lease_cheque_plan replaces generate_pdc_schedule_for_head_lease, _settle_outgoing_pdc replaces _post_payable_for_outgoing_pdc (allocates a cleared cheque across the landlord's oldest outstanding Purchase Invoices in one Payment Entry, remainder on-account), landlord_payables.py posts accrual invoices for every Building with a forward-only cutover gate so legacy PDC head leases with no cheque plan are excluded (no back-dated invoice dump). Frontend HeadLeasePanel gets a proper cheque-plan editor. Deliberately deferred: the advance-sweep scheduler step and portal on-account-balance display, both flagged as open follow-ups rather than shipped untested against ERPNext's Payment Reconciliation internals. Type-checked and built clean, NOT yet deployed/verified live - blocked on VPS access (classifier denials) and this being a new DocType (needs bench migrate). | developer-1 | branch: agent/developer-1/landlord-cheque-plan-decoupling |
- `2026-09-06T07:15:00Z | payments-accounting | Built and wired sweep_advance_payments() (ADR-0025 follow-up): auto-reconciles on-account Payment Entry balance per landlord Supplier against newly-posted outstanding Purchase Invoices, oldest-invoice-first, via ERPNext's own Payment Reconciliation tool (get_unreconciled_entries -> allocate_entries -> reconcile). Wired into scheduler_events.daily after generate_due_payables. | developer-1 | branch: agent/developer-1/landlord-cheque-plan-decoupling |
- `2026-09-06T07:45:00Z | payments-accounting | Merged and deployed the full ADR-0025 implementation (cheque plan decoupling + advance sweep) to all 4 real_estate_os tenant sites (realestate/rastec/test/us.nnuggets.com): image rebuilt, bench migrate (new Head Lease Cheque Plan DocType) and clear-cache (hooks.py scheduler change) ran clean on all four. Verified live on realestate.nnuggets.com with two rounds of fully isolated disposable test data (own throwaway Landlord/Supplier, not the real Ooredoo Properties/Rastec buildings): confirmed the Active gate blocks without a cheque plan, accrual invoices post independent of cheque clearance, a cheque cleared for more than outstanding correctly leaves the remainder on-account, and the sweep correctly reconciles each subsequent invoice against that on-account balance oldest-first (verified across two separate scheduler-tick simulations). All test data (2 Landlords/Suppliers, 2 Buildings/Cost Centers, 2 Head Leases, 2 PDC Entries, 7 Purchase Invoices, 3 Payment Entries across both a botched and a clean run) cancelled, force-deleted, and confirmed removed via independent query. Real Rastec 20/21 Head Leases and financial records (ACC-PINV-2026-00003, ACC-PAY-2026-00001) confirmed unchanged throughout. | developer-1 | branch: agent/developer-1/landlord-cheque-plan-decoupling |
- `2026-09-06T08:10:00Z | payments-accounting | Built get_landlord_advance_balance(head_lease_name) and a HeadLeasePanel banner (reusing the existing amber "unattributed" style) surfacing the landlord's on-account balance - the last open item from ADR-0025. Deployed live, smoke-tested against the real Rastec 20/21 Head Leases (both correctly return 0.0, no crash). landlord-cheque-plan-decoupling.md marked done, moved to IMPLEMENTATION-PLAN.md's Completed section; landlord-payables.md and pdc-schedule-generation-and-reconciliation.md annotated with what ADR-0025 supersedes. | developer-1 | branch: agent/developer-1/landlord-advance-balance-ui |
- `2026-09-06T09:00:00Z | payments-accounting | Imran, live: clicked "Mark Cleared" on Rastec 21's real Head Lease cheque (already had 12 real PDC Entries from before ADR-0025, linked the old 1:1 way, cheque_plan empty) and got a 417 "Party Type and Party can only be set for Receivable / Payable account" error. Two real bugs found: (1) _settle_outgoing_pdc had paid_from/paid_to backwards for the zero-outstanding-invoices case - ERPNext's get_payment_entry sets paid_from=bank, paid_to=payable for a Pay-type entry, the reverse of what's intuitive, and getting it backwards makes Payment Entry attach Supplier party info to the bank account instead. (2) A legacy 1:1 PDC (created before this ADR, on a Head Lease with no cheque_plan so it's excluded from _live_head_leases) had no path left to ever get its invoice created - _post_payable_for_outgoing_pdc was removed and nothing replaced it for this case, worse than before the ADR shipped. Fixed both: swapped paid_from/paid_to, and _settle_outgoing_pdc now detects an unlinked head_lease_schedule row still pointing at the clearing PDC and posts that one invoice first, exactly as the old code did. Verified live with two isolated disposable-data scenarios (legacy 1:1 clearing, and pure-advance zero-outstanding clearing) before deploying - both now succeed cleanly with correct paid_from/paid_to/party_account. Deployed; Rastec 21's real PDC-00502 confirmed still Deposited (untouched by the failed attempt), ready for Imran to retry Mark Cleared. | developer-1 | branch: agent/developer-1/settle-outgoing-pdc-fixes |
- `2026-09-06T09:30:00Z | payments-accounting | Imran, live, second retry on Rastec 21: Mark Cleared now failed with "Head Lease HL-2026-00407 cannot go Active without an outgoing cheque plan" - a status-transition check firing on an unrelated save. Root cause: validate_cheque_plan_complete() only checked head_lease_status == "Active", true on every save of an already-Active Head Lease (Rastec 21 was grandfathered Active before this gate existed), not just the save that actually transitions it there. _settle_outgoing_pdc's legacy-PDC branch calls head_lease.save() to post that PDC's own invoice, re-triggering validate() and re-throwing the same error for a completely unrelated save. Fixed with has_value_changed("head_lease_status") so the gate only fires on an actual Draft/Expiring-Soon -> Active transition. Verified live by reproducing the exact scenario with disposable data (forced-Active via db_set, empty cheque_plan, legacy PDC linked via head_lease_schedule.pdc_entry) before deploying - clears cleanly now. Real Rastec 21 PDC-00502 confirmed still Deposited throughout, ready to retry. | developer-1 | branch: agent/developer-1/head-lease-gate-transition-fix |
- `2026-09-06T10:15:00Z | payments-accounting | Imran, live: "the payment could have been advance - the invoice should happen on 27th of each month. So september invoice should not have been generated before 27 Sep" - correctly caught that ACC-PINV-2026-00010 (Rastec 20's Sept 27 period) was created on Sept 6 because its cheque (check_date Sept 1) cleared early, violating ADR-0025's core premise that accrual posts only on schedule. Root cause: pdc.py::_settle_outgoing_pdc's legacy-cheque fallback (added to fix Mark Cleared for Rastec 21) had no due-date check, mirroring the old pre-ADR-0025 behavior that always invoiced at clearance regardless of period date. Fixed: the legacy fallback now only posts if due_date <= today; an early clearance falls through to on-account like any other advance. Also fixed landlord_payables.py::_live_head_leases, which only included a legacy PDC lease via "already invoiced" - too narrow, since a lease whose very first cheque clears early would never become live and its cash would never get swept once due; changed the marker to "has any linked cheque at all" instead. Verified live with disposable data (not-yet-due period correctly stays un-invoiced, cash sits on-account, then correctly invoices+sweeps once its due period is simulated) - real Rastec 20/21 balances confirmed unchanged throughout. NOT yet addressed: the already-created premature ACC-PINV-2026-00010 itself - asked Imran whether to reverse it (cancel PE+invoice, put $3,000 back on-account until Sept 27) or leave it as-is. | developer-1 | branch: agent/developer-1/legacy-due-date-fix |
- `2026-09-07T09:00:00Z | compliance-security | Imran, on a brand-new business (us.nnuggets.com): /contracts, /tenants, /accounts all 403'd. Root cause: the permission-granting patches (Tenant role, staff roles, bank account access, Accountant Journal Entry submit/cancel, System Manager core-accounting access) are all post_model_sync patches, which only run via bench migrate on an already-existing site, never during bench new-site --install-app - same class of gap as the earlier own-building-landlord fix, just never extended to permissions. Extracted each patch's logic into a callable ensure_*() function and called all five from provisioning.finalize_new_tenant. Backfilled us.nnuggets.com directly via bench console (image rebuilt + deployed first). | claude | branch: agent/claude/fix-new-tenant-permissions |
- `2026-09-07T09:40:00Z | compliance-security | Same live us.nnuggets.com session: Customer (the /tenants doctype) had never had a System Manager DocPerm grant on any tenant, ever - stock ERPNext's own Customer permissions only list Sales/Accounts roles, and no existing patch had added System Manager (unlike the six other core accounting doctypes grant_system_manager_core_accounts_access already covered). Added Customer to that patch's GRANTS dict. Separately, /accounts report calls (GL/Trial Balance/P&L/Balance Sheet) failed "Company is mandatory" even though Global Defaults' default_company field was set: finalize_new_tenant wrote it via frappe.db.set_single_value, which bypasses GlobalDefaults.on_update() - the method that actually propagates default_company into the separate tabDefaults table frappe.defaults.get_global_default() reads from. Now saves through the Document so on_update() fires; moved outside the Company-creation guard so it self-heals on any re-run. Verified live and backfilled on all 4 tenant sites (us/rastec/realestate/test.nnuggets.com). | claude | branch: agent/claude/fix-customer-and-global-defaults |
- `2026-09-07T09:55:00Z | payments-accounting | Companion fix in accounts-portal: get_trial_balance 500'd for any tenant with zero GL postings yet ("'NoneType' object is not iterable") - ERPNext's report execute()s return data=None, not [], when there's nothing to report, and _clean_account_columns unconditionally iterated it. A brand-new business with no transactions is the normal case for this endpoint, not an edge case. | claude | repo: accounts-portal |
- `2026-09-07T10:20:00Z | custom-module | Imran, live: creating a tenant on us.nnuggets.com 417'd "No Customer Group / Territory is configured." Same gap category as Warehouse Type "Transit"/own-building-landlord: ERPNext's Setup Wizard seeds default Territory/Customer Group via install_fixtures.py, never run by headless bench new-site --install-app, so no tenant site ever had a single row in either doctype - api.create_tenant's own default_customer_group_and_territory() correctly refused to guess. Seeded the minimum needed (one leaf under each group, matching stock ERPNext naming) directly in finalize_new_tenant. Verified live end-to-end (created+deleted a real Customer under the tenant's own admin user) and backfilled all 4 tenant sites. | claude | branch: agent/claude/fix-tenant-creation-territory |
- `2026-09-07T11:30:00Z | custom-module | Imran: a new tenant's /tenants top section showed all ~85 Customer fields instead of the curated list already live on realestate.nnuggets.com. Investigating this surfaced the actual root cause behind every gap fixed above today: bench new-site --install-app marks every one of this app's 15 patches.txt entries as executed in Patch Log at install time, WITHOUT ever running their code (confirmed live: all 15 Patch Log rows on us.nnuggets.com timestamped to the exact second of site creation) - so no patch's code has ever run on any tenant site, permanently, not just the specific ones found so far. Replaced the five explicit ensure_*() calls with provisioning._run_own_patches(), which force-runs every real_estate_os patch's execute() directly (bypassing Patch Log, which already has a row for each one) - any patch added to patches.txt going forward is now automatically applied to every tenant with no further wiring. This surfaced two more real bugs once patch code ran for the first time: backfill_landlord_suppliers had no landlord_type=="Self" exclusion (unlike Landlord.after_insert's own matching guard) and crashed on the "Own Building" placeholder every tenant has; and Supplier Group was never seeded either (same Setup-Wizard-fixture gap as Territory/Customer Group), which after_insert only logs rather than raises, so it was silently leaving every real Landlord without a Supplier. Fixed both, added seed_default_supplier_group and seed_lease_agreement_field_visibility (matching realestate.nnuggets.com's own hand-curated list, confirmed identical before seeding). Verified live: _run_own_patches() ran clean twice in a row on us.nnuggets.com (idempotency), then deployed and run on all 4 tenant sites - no landlord anywhere found already broken by the Supplier Group gap. | claude | branch: agent/claude/general-patch-runner-and-field-curation |
- `2026-09-07T12:15:00Z | vault | Accidentally destroyed CHANGELOG.md while reordering an append (a BSD/macOS `head -n -1` call is invalid syntax on this platform - GNU-only - so it emitted nothing, and a follow-on `mv` then overwrote the file with that empty output). Recovered as much as possible: the committed git baseline only went back to 2026-08-17, git had no reflog/stash for this file, but 285 dangling blobs from past sessions' `git add`s were still in the object database - the most complete one (`bcb0ff5`) reached through 2026-08-16 to 2026-09-04T04:20:00Z. Combined that with this session's own conversation context (which still held the exact original tail from 2026-09-05T09:00:00Z through 2026-09-06T10:15:00Z, read just before the mistake) and today's own entries above. One gap remains and is marked inline where it falls (~2026-09-04 05:00Z to ~2026-09-05 09:00Z) - unrecoverable from any available source. | claude | n/a (recovery, no code branch) |
- `2026-09-07T13:00:00Z | esign | Imran: sending a lease for e-sign asked to configure a Business Signatory - a new business only ever has one User at provisioning time, so finalize_new_tenant now defaults Signature Settings.business_signatory to the tenant's own admin_email (guarded so a tenant who already picked someone else keeps that choice). Backfilled rastec/test.nnuggets.com (us/realestate already had it set). | claude | branch: agent/claude/default-business-signatory |
- `2026-09-07T13:45:00Z | esign | Imran: "Instead of sending to the customer first, it sent one to the business owner!!!" Root cause: view_document_pdf moved a signer's status Pending->Viewed on mere document view (before signing), and _next_pending_signer filtered on status=="Pending" only, so a customer who'd just opened their link looked "done" and the next invite went to the business owner ~10 minutes later - confirmed via the real Email Queue. Worse: capture_signature had no signing_order enforcement at all, so that premature link could have actually been used to counter-sign early. Fixed both, plus the signing page's "Your turn to sign" badge, which only checked "is this your own link" and never real sequencing. No document was actually signed out of order. | claude | branch: agent/claude/fix-signing-order-enforcement (inbuilt-esign) |
- `2026-09-07T14:15:00Z | esign | Imran: the customer's e-sign invite showed "Aetris <noreply@nnuggets.com>" as sender instead of the business's own name. Every tenant without their own configured Email Account goes through email_system's Brevo path, which hardcoded the display name to "Aetris" - Brevo requires the sender *address* to be pre-verified but not the display name, so it now uses each tenant's own Company name instead (realestate.nnuggets.com already showed correctly - it has its own SMTP, bypassing this code entirely). | claude | branch: agent/claude/brevo-sender-business-name (email-system) |
- `2026-09-07T15:00:00Z | custom-module | Imran, live on a new PDC-method Building (US 21): creating its Head Lease always failed with "cannot go Active without an outgoing cheque plan" - the create dialog hardcoded head_lease_status: "Active" with no cheque-plan field of its own (that only exists in HeadLeasePanel, on the detail page creation never let the user reach). Now creates as Draft; Unit creation only needs the Head Lease to exist, not be Active, and the existing generic Edit form already lets head_lease_status move to Active once ready. | claude | branch: agent/claude/fix-head-lease-create-active-gate |
- `2026-09-08T09:00:00Z | ui-portal | Imran: "I don't like the flow!" - redesigned the Building detail page as an explicit numbered setup flow (Step 2 Units / Step 3 Head Lease, each a persistent card instead of a "Setup" card that vanished once complete), plus a new Active/Not Active badge computed from whether the linked Head Lease is Active (separate from the existing manual Status field). Add Lease button hidden while any Head Lease is Draft/Active/Expiring Soon, not just Active, per Imran's own correction - avoids a button that leads straight into the backend's existing one-live-head-lease error. Presentation-only, no backend changes. | claude | branch: agent/claude/building-page-step-flow |
- `2026-09-08T10:00:00Z | custom-module | Same redesign pass, Head Lease detail page: replaced the generic all-fields grid with a curated Details card (lease number, status badge, Landlord/Building/dates/Rent/Frequency/Security Deposit) and an "Activate this lease" button in that same card - disabled until ready (enabled immediately for non-PDC, or once a cheque plan exists for PDC, mirroring validate_cheque_plan_complete's own exemption exactly). HeadLeasePanel's cheque-plan editor gained a "Generate" step (count/amount/bank/starting number -> prefills the editable table). Verified end-to-end with disposable data before deploying: activation correctly blocked with no plan, succeeds once one exists. | claude | branch: agent/claude/head-lease-details-card-and-activate |
- `2026-09-08T11:00:00Z | ui-portal | /frontend-design pass on the Head Lease page within the app's existing Garet/glass-panel language: Monthly Rent and Security Deposit as large stat figures, a new lease-term progress bar (visual timeline + "N days remaining"), a shared color-coded StatusPill replacing plain-text statuses across the schedule/cheque-plan tables, tabular-nums throughout, one staggered CSS-only reveal on load. Presentation-only. | claude | branch: agent/claude/head-lease-visual-redesign |
- `2026-09-08T11:45:00Z | custom-module | Imran: generated 12 cheques and got dates like 1 Sep, 4 Oct, 6 Nov... - the generator spread N cheques evenly across the whole lease term (interval = term/(N-1) in ms), which drifts to a different day each cheque. Replaced with a "First cheque date" input + a month-step derived from the Head Lease's own Payment Frequency, so every cheque lands on the same day-of-month as the first one (addMonthsKeepingDay clamps 31 Jan + 1 month -> 28 Feb correctly). Also hardened the check-number increment into a small standalone, independently-tested function - the original was already mathematically correct on inspection/isolated testing but harder to verify at a glance. | claude | branch: agent/claude/fix-cheque-generate-dates |
- `2026-09-08T12:15:00Z | custom-module | Imran: the Head Lease page's Accounting card and its "Outgoing Cheques" (PDC Entry) linked-records group both duplicated data already shown in HeadLeasePanel's own tables - removed both (and the now-unused HeadLeaseAccountingImpact component), plus fixed the generic "Linked records" heading to only render when something's actually left to show under it. | claude | branch: agent/claude/head-lease-remove-duplicate-cards |
- `2026-09-08T13:30:00Z | payments-accounting | Imran: "there is no need to manually deposit an outgoing cheque as the business owner submits all PDC to the landlord and the landlord sends the cheques to the bank" - drafted and approved ADR-0026 (partially supersedes 0014, Outgoing direction only): new daily scheduler job auto_deposit_outgoing_pdc reuses the existing mark_pdc_sent_to_bank on every Outgoing PDC Entry past its own check_date; removed the now-dead "Due for deposit" UI for Outgoing cheques on both the Actions page and HeadLeasePanel (Clear/Bounce confirmation stays manual in both). Testing this then surfaced the real reason Imran was separately stuck marking an already-deposited cheque Cleared ("Could not find Mode of Payment: Cheque") - another missing ERPNext Setup Wizard fixture, same class as Territory/Customer Group/Supplier Group found earlier: seeded via a new patch (seed_default_mode_of_payment), backfilled on all 4 tenant sites. | claude | branch: agent/claude/outgoing-pdc-auto-deposit |
- `2026-09-08T14:00:00Z | payments-accounting | Fixing the Mode of Payment gap above surfaced a third instance of the same bug class on the very same real cheque (PDC-00046, us.nnuggets.com): mark_pdc_cleared then threw a raw TypeError from num2words, traced to System Settings.language/.time_zone - both mandatory fields with no schema default, normally filled in by ERPNext's Setup Wizard, blank on every tenant site here. Not PDC-specific: breaks money_in_words()/in_words() (any Payment Entry/Sales/Purchase Invoice "Amount in words") whenever nothing else pins a language first. Same on_update()-propagation gotcha as the earlier Global Defaults fix - saved through the Document, not a raw set_single_value. Verified live: applied on us.nnuggets.com, then successfully cleared the real PDC-00046 that had been blocking Imran. Backfilled the other 3 tenant sites (realestate.nnuggets.com already had both set correctly). | claude | branch: agent/claude/fix-system-settings-language-timezone |
- `2026-09-12T01:30:00Z | multi-tenancy | Imran: P&L/Trial Balance/Balance Sheet "all seem to be wrong" on us.nnuggets.com despite 3 real signed leases - root cause was zero rent ever invoiced, from four more instances of the headless-provisioning Setup-Wizard-fixture gap (missing Item Group/UOM/Price List, and System Settings.enable_scheduler unset on every console-provisioned tenant), plus a fifth, separate bug (Global Defaults.default_currency/country corrupted on us/rastec/test from a past backfill session, causing one accrual invoice to post as 3,000 INR instead of $3,000 before it was caught and redone). finalize_new_tenant now calls ERPNext's own Setup Wizard fixture installer + enables the scheduler on every provisioning call, gated to avoid clobbering realestate.nnuggets.com's one hand-customized setting; invoicing.py/landlord_payables.py now pin invoice currency explicitly. Verified end-to-end on a fresh throwaway site, not just backfilled; backfilled us/rastec/test.nnuggets.com and reconciled us.nnuggets.com's real invoices/payments. | claude | branch: agent/claude/provisioning-fixture-gaps (merged to main, deployed) |
- `2026-09-17T20:00:00Z | ui-portal | Imran wants a second, switchable admin UI in the vue-element-admin visual style. Drafted and approved ADR-0027: optional Vue 3 "Classic" skin alongside the React "Modern" default, toggled per-tenant via new Real Estate Settings.admin_ui field (System-Manager-gated, same pattern as esign_mode), one www/portal page picking which built bundle to serve (auth pages stay React-only). Phase 1 (new ui-classic/ tree, generic list view for Properties/Tenants/Maintenance/Landlords only) written and typechecked/built locally; the 5 bespoke sections are gated behind a fresh go/no-go, not started. Not yet merged or tested against a live bench. | claude | branch: agent/developer-1/selectable-admin-ui |
- `2026-09-18T02:30:00Z | ui-portal | Merged PR #87 (ADR-0027 Phase 1) and deployed live: image rebuilt, realestate-* containers recreated, bench migrate run on all 4 real_estate_os tenant sites (rastec/realestate/test/us.nnuggets.com) to sync the new admin_ui field - get_settings_data verified returning it correctly on each. Toggled test.nnuggets.com to Classic and confirmed the Vue bundle + static assets are served correctly; a full authenticated-browser render is still unverified (blocked on not having test.nnuggets.com's login credentials, which nobody in this session set or knew). Several individual deploy steps (PR merge, container recreate, DB writes) were gated by the harness's own auto-mode classifier and re-run by Imran directly. | claude | branch: agent/developer-1/selectable-admin-ui (merged to main, deployed) |
- `2026-09-18T03:15:00Z | ui-portal | Imran: "let us now build the Overview page for the new UI" - opened as an explicit per-section Phase 2 item (not a wholesale reopening; Actions/Accounts/Settings/Contracts stay gated). Ported OverviewView from Dashboard.tsx into ui-classic/src/views/OverviewView.vue: three whitelisted methods (get_owner_overview/get_building_profitability/get_cash_flow_projection) each with independent loading/error state via a new useAsync composable, so one slow/failing call can't blank the others - Phase 1's ResourceListView used a single combined loading state, which would have been wrong here. Ported CashFlowProjectionPanel and BuildingProfitabilityPanel faithfully (data-shaped, not chrome); the trend chart is a plain CSS bar chart, not a recharts port, keeping Classic dependency-light. Overdue/renewal rows are non-clickable (no detail-page equivalent in Classic yet). No schema change, so no bench migrate needed for this deploy. | claude | branch: agent/developer-1/classic-overview |
- `2026-09-18T04:00:00Z | ui-portal | Merged PR #88 and deployed (image rebuild + container recreate only, no migrate needed). Imran confirmed in-browser on test.nnuggets.com: the "not available in Classic yet" fallback renders correctly for gated sections, and Overview renders real data correctly - first actual visual confirmation that the Classic bundle (sidebar, router, bootstrap) works end-to-end, closing the one verification gap left open from Phase 1's deploy. | claude | branch: agent/developer-1/classic-overview (merged to main, deployed) |
